# Upstream story fixtures

Generated by `node scripts/capture-upstream-stories.mjs`. Do not edit by hand.

- Upstream: https://github.com/civictheme/uikit `packages/twig`, commit `fe4291907b1ea15cfc0ea5d9ca47d31964a5b91a`
- Captured: 2026-09-09
- Storybook: `npm run build-storybook --workspace=packages/twig` (static build), served over HTTP
- Args: read at runtime from the Storybook preview store
  (`window.__STORYBOOK_PREVIEW__.storyStore.loadStory({ storyId }).initialArgs`).
  This gives the exact args the story rendered with, including nested HTML
  strings that `*.stories.data.js` produces by calling other Twig templates.

- Stories in Storybook index: 126
- Captured stories (as upstream defines them): 108
- Extra dark-theme captures (`--dark`, via `&args=theme:dark`): 63
- Total fixtures: 171

## Skipped stories

- `welcome--welcome` — storybook-only template (*.stories.twig)
- `about-civictheme--about-civic-theme` — storybook-only template (*.stories.twig)
- `base-colors--colors` — storybook-only template (*.stories.twig)
- `base-fonts--fonts` — storybook-only template (*.stories.twig)
- `base-typography--typography` — storybook-only template (*.stories.twig)
- `base-background--background` — storybook-only template (*.stories.twig)
- `base-elevation--elevation` — storybook-only template (*.stories.twig)
- `base-grid--grid` — story declares render:
- `base-grid--grid-demo` — story declares render:
- `base-spacing--spacing` — storybook-only template (*.stories.twig)
- `base-utilities-collapsible--collapsible` — storybook-only template (*.stories.twig)
- `base-utilities-flyout--flyout` — storybook-only template (*.stories.twig)
- `base-utilities-responsive--responsive` — storybook-only template (*.stories.twig)
- `base-utilities-scrollspy--scrollspy` — storybook-only template (*.stories.twig)
- `atoms-table--table` — story declares render:
- `molecules-back-to-top--back-to-top` — storybook-only template (*.stories.twig)
- `organisms-alert--alert-api` — story declares render:
- `organisms-navigation-mobile-navigation--mobile-navigation` — story declares render:

## Capture options

- Viewport: 1024x900, device scale 1 (CSS pixels).
  1024 wide instead of upstream's 1280 to keep the committed PNG set small.
- PNG is an element screenshot of `#storybook-root` (full element height).
- Mirrors `tools/visual-diff`: CSS transitions disabled, .ct-iframe, .ct-map--canvas, .ct-video-player, .ct-video, video hidden,
  every image replaced with a 1x1 PNG, 2000 ms settle delay.
- HTML is the raw `#storybook-root` innerHTML, unnormalised.
