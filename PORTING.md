# Porting notes

One row per component where the Twig → Astro port needed a decision.

<!-- prettier-ignore-start -->
| Component | Twig construct | Astro decision |
| --- | --- | --- |
| Paragraph | `{{- content -}}` (no `\|raw` in the twig, but the doc header types `content` as `[string,null]` HTML-bearing) | Rendered with `set:html` per R2's "content / html-bearing string props" rule, not a plain text child. No `allow_html` prop exists upstream, so none was added — the task brief's illustrative example included one that does not match the real Twig. |
| Paragraph | `no_margin: [boolean,null]` | Astro prop `noMargin?: boolean \| null`, default `false`; only appends `ct-paragraph--no-margin` when true. Not exercised by an upstream snapshot, so parity for this branch rests on the class-list logic mirroring the twig, not a captured case. |
| (harness) | `vitest.config.ts` per Task 3 Step 1 | `astro check` does not know vitest's `test` field on `UserConfig` from `astro/config` (vitest 3.x no longer ambient-augments `vite`'s `UserConfig`). Fixed by excluding `vitest.config.ts` from the root `tsconfig.json` (`"exclude": ["dist", "vitest.config.ts"]`) rather than weakening the file's own types. |
| (harness) | Void/self-closing elements (`<img>`, `<br>`) not yet ported | `normaliseHtml`'s `walk()` always emits a closing tag (`</${tag}>`). This is harmless today because both `actual` and `expected` go through the _same_ `walk()`, so the extra closing tag cancels out on both sides of the comparison — but a real DOM diff tool would show something different. Revisit when the first void-element component is ported. |
| (harness) | Upstream's `assertUniqueCssClasses(c)` (jest helper, checks the rendered class list has no duplicate tokens) | linkedom's parser doesn't error or warn on duplicate class tokens, and `normaliseHtml`'s class handling (`split` → `filter(Boolean)` → `sort` → `join`) silently de-duplicates nothing — a genuine duplicate class token would still appear twice in the sorted, joined string and would fail parity against upstream's (also duplicate-free) snapshot. There is no direct equivalent assertion in this harness; duplicate classes would only be caught if they change the compared string, which they do. No action needed, but noting there's no dedicated "no duplicate class" check, only the indirect one via string equality. |
| (harness) | HTML entities (`&amp;`, etc.) in snapshot or rendered content | No special-case code: `linkedom`'s `parseHTML` decodes entities into `textContent` during parsing, so `&amp;` and a literal `&` both normalise to the same text node content. Confirmed by a self-test (`harness.test.ts`). |
| (harness) | linkedom `parseHTML('<body>…')` on a fragment | `parseHTML` requires a _complete_ document; handing it a bare `<body>...</body>` (or any fragment) string yields a `document` whose `body` is empty, silently dropping all content. Every call site (`normaliseHtml`, `unwrapSnapshotHtml`) wraps input in `<!DOCTYPE html><html><body>...</body></html>` before parsing — never pass a fragment straight to `parseHTML`. |
| (harness) | Whitespace collapse must never touch already-serialised markup | `normaliseHtml`'s whitespace-collapse regex (`\s+` → ` `) runs only on raw _text node_ content, never on a joined-children string that contains child elements' serialised tags/attributes — running it there would also rewrite whitespace inside those children's attribute values. Per-element edge trimming (`.trim()` on the joined children) is stricter than plain rendered-text equivalence (it also discards meaningful edge whitespace a browser would keep in inline flow) — accepted by design; see the self-tests in `harness.test.ts` for the exact boundary. |
| (harness) | Jest's pretty-format DOM serializer renders ANY childless element self-closed (`<tag ... />`) as a display convention, even for non-void HTML tags like `<a>` | Outside foreign content (svg/math), a trailing `/` before `>` on a non-void tag is not real self-closing syntax to an HTML parser — it is ignored, and the parser treats everything textually following as nested inside that (still-open) element until a real `</tag>` appears. First hit by `Menu`'s empty collapsible-trigger `<a></a>` (upstream: `renders collapsible menu`) sitting next to a following sibling `<div>`. Fixed generally in the harness: `upstreamSnapshot` now runs `fixNonVoidSelfClosingTags` (rewrites `<tag ... />` → `<tag ...></tag>` for any tag not in the standard void-element set) before parsing, so a `.snap` fixture's self-closed empty tags parse the same way a real browser parsed them the first time (as immediately-closed, not mis-nesting whatever follows). |
| Icon | `source(assets_dir ~ '/icons/' ~ symbol ~ '.svg', true)` then `source\|replace({'<svg ': '<svg ' ~ attr ~ ' '})\|raw` | Replaced by the generated `icons.ts` symbol map (Task 2); `Icon.astro` looks up `icons[symbol]` and does the identical `'<svg '` → `'<svg ' + attrs + ' '` string replace so the injected attribute set (`class`, `aria-hidden`, `role`, then rest) lands in the same position upstream's Twig `replace()` puts it. Attribute order does not matter (the normaliser sorts attributes), only presence/values. |
| Datetime | `{{ start_iso\|default(start)\|striptags }}` | `stripTags = (s) => s.replace(/<[^>]*>/g, '')` applied to `startIso ?? start` / `endIso ?? end` for the `datetime` attribute, while the visible `<time>` content keeps the original (possibly HTML-bearing) string via `set:html` — matches the "strips HTML tags from attribute values" snapshot, where the attribute is stripped but the rendered content is not. |
| Datetime | `&mdash;` between start/end `<time>` elements, surrounded by Twig whitespace | Rendered as the literal `{' — '}` text node (with surrounding spaces) rather than an HTML entity — Astro does not decode `&mdash;` in plain template text, and the harness's entity-vs-literal-character equivalence only applies to text parsed from HTML strings, not to what Astro serializes as literal JS/JSX text. |
| Grid | `template_column_count`/`auto_breakpoint` column-class algorithm (breakpoint map keyed 1,2,3,4,6, deliberately skipping 5, with a special-case skip at `template_column_count==6 && i==4`) | Ported as a literal `for` loop over `1..validCount` replicating the same skip conditions, rather than a "clever" derived formula — the upstream algorithm's odd asymmetry (level skip at 6/4) is not obviously generalizable and a byte-for-byte translation is safer than a reinterpretation. |
| Grid, ItemList, Menu | Sibling elements produced by a Twig `{% for %}` loop always have inter-element whitespace (from source newlines/indentation) that survives the harness's whitespace-collapse-per-text-node rule | Astro's `items.map(...)` produces adjacent JSX elements with _no_ text node between them, which normalises to no separator at all — while upstream has a collapsed-to-one-space text node between siblings. Fixed by interleaving an explicit `{i > 0 && ' '}` text node between mapped siblings (columns, list items, menu items, and the sub-menu wrapper after a parent link) everywhere the Twig source loops. This is now a recognised pattern, not incidental. |
| Grid | Three distinct "attributes" targets in one component (`attributes` → container-or-row, `row_attributes` → row always, `column_attributes` → column always) | Modelled as three separate props (`...rest` for `attributes`, plus explicit `rowAttributes`/`columnAttributes` objects) rather than a single rest-spread, since R2's "attributes → rest spread onto root" assumes one root target; Grid's Twig has three. `rest` is applied to the container div when `useContainer`, or merged onto the row when not (mirrors `row_attributes.merge(attributes)`). |
| Layout | `content`, `content_top`, `content_bottom`, `sidebar_*` are plain Twig variables but are documented under a "Slots:" doc-header section | Translated to Astro named slots (`default` for `content`, camelCase names for the rest) per R2's slot rule, rather than string props — `Astro.slots.has(...)` stands in for Twig's `is not empty` gates. `hasSidebarLeft`/`hasSidebarRight` (and therefore `isContained`, `verticalSpacing`, and every `no-*-class`) are derived from slot presence, not a content string. |
| Menu | `{% import _self as menus %}` + two mutually-recursive macros (`menu_links`, `menu_links_below`) parameterised by `menu_level` | Single recursive `Menu.astro` importing itself (`import Menu from './Menu.astro'`), branching on a `menuLevel` prop (0 → bare `<ul>`, `>0` → `<div class="...wrapper">` + nested `<ul>`) — mirrors the macro's own `{% if menu_level == 0 %}` branch instead of keeping two separate templates. |
| Menu | Upstream's per-level `aria_hidden`/`data-collapsible-panel` wrapper attributes read `item.in_active_trail` from the _enclosing_ `for` loop's `item` variable (a `_self`-import macro-scoping quirk — `item` is not a macro parameter), not from any property of the `below` items being rendered | Reproduced by passing an explicit `parentItem` prop down one recursion level (the parent item whose `.below` is being rendered), and reading `parentItem?.inActiveTrail` for the wrapper's `aria-hidden`, rather than relying on any implicit scoping (Astro components have no equivalent leaked-scope behaviour, nor should they). |
| Menu | `@atoms/link/link.twig` include for each menu item's link, which itself includes `@base/text-icon/text-icon.twig` | **Task 7 update: uses `Link`.** `Menu.astro` imports the ported `Link` atom and passes it the same props `menu.twig` passes to `@atoms/link/link.twig` (`theme`, `text`/`title` = `item.title`, `url` = `item.url`, `modifier_class: 'ct-menu__item__link'`, `is_new_window`/`is_external` defaulting to `false`, plus the item's link attributes as rest). All 11 Menu parity cases stayed green with no expectation changes. |
| Menu | Empty `<a href="#" ...></a>` collapsible-trigger, immediately followed by a sibling `<div>` | First component to have a genuinely-childless non-void element next to a following sibling — triggered the `fixNonVoidSelfClosingTags` harness fix documented above (Menu's `renders collapsible menu` snapshot is what surfaced it). |
| TextIcon | No upstream `.test.js` / `__snapshots__` for `text-icon` | Given a smoke test only (root `.ct-text-icon__text` span), per the task brief's rule for components without an upstream test. Implemented the full Twig logic (icon placement, external-link icon, grouped last-word + icon, new-window visually-hidden text) as best-effort fidelity since `Menu` depends on TextIcon's simple (no-icon) branch for its own parity, but the icon/external/grouping branches are not snapshot-verified anywhere in this template. |
| TextIcon | `attributes` prop documented in the Twig doc header but never referenced anywhere in the Twig body | Accepted as a documented-but-intentionally-unused prop is not applicable here — no `attributes` prop was added to `Props` at all, since adding one and not using it would be a nonsensical API surface; the doc comment instead records that upstream itself never applies it. |
| Grid | `row_attributes = row_attributes.merge(attributes)` (Drupal's `Attribute::merge()`, used only when `use_container` is false) | Ported as a plain object spread, `{ ...rest, ...rowAttributes }` (`rowAttributes` wins on key collision). Drupal's `Attribute::merge()` has its own precedence/append rules (e.g. array-valued attributes like `class` are appended rather than replaced), which are not reproduced here — no upstream snapshot exercises `attributes` and `rowAttributes` sharing an overlapping key, so this divergence is undetected by parity today. Revisit if a future case needs Drupal's exact merge direction. |
| (harness) | `fixNonVoidSelfClosingTags`'s regex (`/<([a-zA-Z][a-zA-Z0-9-]*)((?:\s+[^<>]*?)?)\/>/g`) | Assumes an attribute value never itself contains a literal `>` character — true of every `.snap` fixture pinned in this repo today, but a future upstream snapshot with e.g. a `title="a > b"`-style attribute value ahead of a self-closed non-void tag could confuse the attribute-capture group. Flagging rather than hardening against a case that cannot currently arise from any pinned fixture. |
| Layout | Seven Twig `{% block %}` markers (`content_top_block`, `content_block`, `sidebar_top_left_block`, `sidebar_top_right_block`, `sidebar_bottom_left_block`, `sidebar_bottom_right_block`, `content_bottom_block`) | Per R2, blocks render inline with no override mechanism in Astro — dropped. None of Layout's own upstream tests rely on a Drupal sub-theme overriding one of these blocks (they exist purely for downstream Drupal theme customisation), so no parity case is affected, but any future consumer expecting to override e.g. just the "content" section independently of the rest of Layout cannot do so with this port. |
| (harness) | **[Superseded by the row below — kept for history.]** Two sibling elements with no rendered text between them, produced by _any_ Twig construct (not only `{% for %}` loops). The first-pass fix ("Jest's pretty-format DOM plugin lays out sibling element nodes on separate indented lines in the `.snap` file regardless of whether a real whitespace text node separates them, so parsing that indentation back always yields a phantom space") turned out to be **factually wrong**: pretty-format does NOT print a padding line for two nodes it renders back-to-back with nothing between them — verified with a live `jest`+`pretty-format`+`jsdom` run (see the harness row below). The original fix (adding `{' '}` at every sibling boundary uncovered by a snapshot) got most of these right by accident/re-verification, but at least three were wrong (`TextIcon`'s text-span→group-span and group-span→isNewWindow-span boundaries needed NO space, not one) and were only caught once the harness itself was fixed to actually distinguish the two cases. See the corrected row below and the `TextIcon`/`Menu.parity.test.ts` fixes it drove. | — |
| (harness) | Distinguishing a real whitespace text node from Jest pretty-format's own indentation-only formatting, when re-parsing a `.snap` fixture's pretty-printed text back into HTML | Verified empirically (a small standalone `jest`/`pretty-format`/`jsdom` script, not by reading the Twig source — see below) that pretty-format prints a real whitespace text node's own content as one raw line per embedded `\n` in that content (with the current nesting indent prepended), and prints **no line at all** between two nodes it lays out back-to-back with nothing between them — it does not add filler/padding lines for readability. So: a line that is whitespace-only (after dropping its trailing newline) is one line of a real whitespace text node → collapses to a single space; any other line is real markup with pretty-format's own nesting indentation prepended, which must be stripped (keeping the rest — including a text line's own trailing space — verbatim); joining every line back together with **no** separator (not a space, not the newline) reconstructs what a browser actually parsed. Implemented as `dedentPrettyPrintedHtml`, run in `upstreamSnapshot` before `fixNonVoidSelfClosingTags`/parsing. One wrinkle: HTML5 does not require whitespace between two attributes (the previous one's closing quote already ends its value) but DOES require it between a tag name and its first attribute — `dedentPrettyPrintedHtml` special-cases a line that is nothing but a bare `<tagname` (pretty-format's convention whenever a tag has attributes on the following lines) by appending a single space, or `<a\n  href="#"\n>` would wrongly dedent to `<ahref="#">`. Self-tested directly (`dedentPrettyPrintedHtml(...)` unit tests) and against two new hand-authored fixture `.snap` entries shaped like real pretty-format output (`tests/parity/fixtures/.../fixture-atoms/spacing/`). The pre-existing per-element edge trim in `normaliseHtml` (leading/trailing whitespace inside an element's own children, collapsed by `.trim()`) is unchanged and still means a text node's real leading/trailing whitespace is unverifiable **at the very edge** of its parent's children (first child's leading space, last child's trailing space) — only whitespace **between** two non-edge siblings is actually checked. |
| TextIcon | Re-verified every sibling boundary against real `.snap` fixtures once the harness's dedent fix could actually tell "real space" from "no space" apart (see harness rows above) | Two of the boundaries added during the initial 01-atoms port turned out backwards: (1) the "before" icon and the text span DO need a space between them (`{iconPlacement === 'before' && resolvedIcon && (<Fragment><Icon .../>{' '}</Fragment>)}`) — confirmed by Button's own upstream snapshot. **Resolved** (was flagged unresolved in an earlier pass): `icon.twig` prints the raw SVG asset file's contents via `{{- source | replace(...) | raw -}}`, and every icon SVG file under `packages/twig/assets/icons/*.svg`ends with a trailing`\n`after its own`</svg>`(confirmed by reading the raw file bytes). Twig's`{%-`/`-%}`trim markers only ever strip literal whitespace written in the *template source* around a tag — they never touch the runtime *value* a`{{ }}`prints, so that trailing newline survives as part of`source`'s string value and becomes a real trailing text node after the icon's `<svg>`. This project's `icons.ts`(vendored in an earlier task, out of this task's scope) stores each SVG's content *without* that trailing newline, so the`{' '}`here is a deliberate, understood compensation for that vendoring-time trim, not a mystery — restoring the fidelity the original (untrimmed) asset file would have had. The rule to remember: **icon SVG files end with a newline; that newline is real output**, wherever an icon is the first thing printed before more content follows it in the same parent. (2) the text span immediately before the "grouped last word" span must have **no** space (removed the`{' '}`there) — confirmed by Button's "is external", Chip's and ContentLink's own snapshots, and matches`text-icon.twig`'s `{%- if not show_full_text -%}<span class="ct-text-icon__group">`(tightly trimmed, no literal space in the source). (3) the space before the`isNewWindow`visually-hidden span is **conditional**, not unconditional:`text-icon.twig`'s `{%- if show_full_text %} {% endif -%}`only emits its literal single space when`show_full_text`is true (regardless of whether any icon follows it) — the grouped, icon-only, and plain-span branches all trim tightly around`content_markup`'s end, so none of them leave a space before `is_new_window_content`. Modelled as a `spaceBeforeNewWindow = hasIconOrExternal && text && showFullText`flag.`Menu.parity.test.ts`'s hand-authored `wires isExternal/isNewWindow...` expectation (there is no upstream snapshot for this path) was updated to match — it had hard-coded the pre-fix (wrong) spacing. |
| (harness) | `checked="checked"` / `disabled="disabled"` literal-value boolean attributes in some `.snap` fixtures (Chip's `is_selected`, Button's `is_disabled`) vs. bare `checked`/`disabled` in others (Checkbox's `is_checked`) | Astro's own renderer (`htmlBooleanAttributes` regex in `astro/dist/runtime/server/render/util.js`, covering `checked`, `disabled`, `required`, `selected`, `multiple`, `readonly`, etc.) always emits these attributes bare when truthy, regardless of what string value a component passes — so an Astro port can never reproduce a fixture's literal `="disabled"`/`="checked"` text; only Twig, which runs outside Astro's renderer, can. Since presence alone conveys a boolean attribute's meaning to a browser (`checked`, `checked=""` and `checked="checked"` are exactly equivalent per the HTML spec), `normaliseHtml` now canonicalises every recognised HTML boolean attribute's value to `''` on both the actual and expected side before comparing (new `BOOLEAN_ATTRS` set in `harness.ts`, mirroring Astro's own list) — only presence is compared for these attributes, never their literal text. This is a harness-level fix, not a per-component one; it does not touch `aria-*` attributes (`aria-invalid`, `aria-disabled`, …), which are ordinary string-valued attributes and must still compare their literal `"true"`/`"false"` text. |
| Button | `kind` gates `<button>` / `<a role="button">` / `<input type="reset\|submit">`; `attributes` (rest) applies to whichever element renders | Ported as a three-way ternary on the resolved `kind`, each branch spreading `...rest` on its own root element. `disabled` on the `<a>` branch needed a cast (`{...({ disabled: ... } as Record<string, unknown>)}` ) since Astro's `AnchorHTMLAttributes` type has no `disabled` field, even though upstream's `link.twig`-adjacent `button.twig` renders it there for the link kind. `button_content` (the `@base/text-icon/text-icon.twig` include) is delegated to the ported `TextIcon` component with `iconClass: 'ct-button__icon'`, per the task's fidelity rule. |
| Checkbox, Fieldset | `@atoms/label/label.twig` include | **Task 7 update: uses `Label`.** Both `Checkbox.astro` and `Fieldset.astro` now import the ported `Label` atom, passing exactly what `checkbox.twig`/`fieldset.twig` pass to `@atoms/label/label.twig` (Checkbox: `theme`, `content` = `label`, `for` = `id`, `size: 'small'`, `modifier_class: 'ct-checkbox__label'`; Fieldset: `theme`, `tag: 'legend'`, `size: 'large'`, `content` = `legend`, `isRequired`, `requiredText`, `modifier_class: 'ct-fieldset__legend'`). Both components' parity cases stayed green with no expectation changes. |
| Chip | Upstream's test passes `is_disabled: true`, but `chip.twig` never reads an `is_disabled`/`disabled` variable anywhere in its body | No `isDisabled` prop was declared on `Chip`, per the fidelity rule that a declared prop must be read and rendered — a prop the markup ignores is not a real API surface. Confirmed by reading the full `chip.twig` source: no `is_disabled` reference exists. |
| Chip | `attributes` (rest) target differs by `kind`: the inner `<input>` for `kind: 'input'`, but the outer `<a>`/`<span>` for `kind: 'link'`/`'default'` | Modelled as `...rest` spread on whichever element is the real Twig target in each branch (three separate JSX spreads, one per branch), rather than a single rest spread on Chip's conceptual "root" — Grid's three-attributes-targets precedent (see the earlier Grid row) already established that R2's "attributes → rest spread onto the root element" assumes a single root, which does not hold here either. |
| Fieldset | `fields` documented under a "Slots:" doc-header section, but the twig body treats it as a plain HTML-bearing variable (`{{ fields }}`), and every upstream test passes it as a plain string prop | Ported as a `fields?: string` prop rendered with `set:html` (Paragraph's precedent), not an Astro named slot — the doc header's "Slots:" label is documentation-only here; twig has no `{% include %}`/slot mechanism for it, only a `{% block fields_block %}` wrapper (dropped per R2, no snapshot depends on overriding it). |
| Iframe | No `{% if %}` guard around the root `<iframe>` element (unlike every other atom in this task, which all gate on a required prop) | Ported as always-rendering (no conditional wrapper), matching upstream exactly — `url`, `width`, `height` are independently optional (each its own `{% if %}` around one attribute only). |
| Image | `alt` is rendered unconditionally (`alt="{{ alt }}"`, no `{% if %}` guard), including the `alt == '""'` Drupal literal-empty-string edge case | `alt` always renders, defaulting to `''` — confirmed against the "renders with required attributes" snapshot, which has `alt=""` even though the test never passes an `alt` prop at all. The `'""'` (two-character literal) edge case is normalised to `''` before rendering. |
| Fieldset | `fieldset.twig`: `{% set _message_type = message_type | default('error') %}`(then coerced to`'error'`if invalid) is resolved BEFORE being passed to`@atoms/field-message/field-message.twig`'s own `type` prop | The initial port passed `messageType` straight through unresolved, so an unset `messageType` silently picked up `FieldMessage`'s own default (`'information'`) instead of Fieldset's real default (`'error'`) — two components defaulting the same prop name to two different values. Fixed by resolving/validating `messageType` in `Fieldset.astro` itself (mirroring the same enum-clamp pattern already used for `descriptionDisplay`) before handing it to `FieldMessage`. No upstream snapshot passes `message` without `messageType`, so covered by a direct `renderNormalised` assertion (checks the `ct-field-message--error` class and the `close-outline` icon path) rather than `parityCase`. |
| Button | `button.twig`: `value="{{- text -}}"` on the `reset`/`submit` `<input>` is unconditional (no `{% if %}` guard) | The initial port used `value={text}`, which Astro omits entirely when `text` is `undefined` — wrong twice over: the attribute must always be present (even as `value=""`), and its content must be `text` trimmed (`{{- -}}` around a value Twig itself doesn't trim, but the review's reading of intent — matched here as `(text ?? '').trim()`). No upstream snapshot exercises a submit/reset button with no or untrimmed `text`, so covered by a direct `renderNormalised` assertion. |
| Heading | `heading.twig`: `{% set level = level | default('2') %}`— Twig's`default` filter falls back on ANY falsy value (`''`, `0`, `null`, `undefined`), not only a missing one | The initial port used a JS destructuring default (`level = 2`), which only applies when the prop is literally `undefined` — an explicit `level=""` or `level={0}` would produce `<h>`/`<h0>` instead of falling back to `<h2>`. Fixed with `const level = levelProp |  | 2`(JS's` |  | `matches Twig's falsy-fallback semantics closely enough here — no case in this component's prop space where` |  | `'s behaviour diverges from Twig's `default`filter). No upstream snapshot passes an explicit falsy`level`, so covered by a direct `renderNormalised` assertion. |
| (harness) | `dedentPrettyPrintedHtml` stripped ALL leading whitespace unconditionally from every non-blank line, rather than exactly its structural nesting depth's worth | A real text node occasionally prints with LESS leading whitespace than its DOM depth would predict — found in `.upstream/uikit/packages/twig/components/02-molecules/field/__snapshots__/field.test.js.snap:608` (`content: 'Field <em>...</em> has an error'`, rendered as raw HTML via `set:html`-equivalent, puts the `"Field "` text node at a shorter-than-structural indent — pretty-format prints a multi-line text node's own continuation lines with NO structural indent at all, only the text's own whitespace; the `min(expectedIndent, actualIndent)` clamp handles this, so a text node's internal indentation is not itself verifiable by this harness. Blindly stripping the full expected amount from every line silently ate into that "Field" text itself (`</em>has an error` instead of `</em> has an error`) — invisible today (no `02-molecules` component is ported yet) but would have forced a wrong `Field` port once one is. Fixed by tracking nesting depth line-by-line (a bare `<tagname` line, its attribute lines at depth + 1, the line closing it with `>`/`/>`, a whole `<tag ...>`/`<tag .../>` on one line, and a `</tagname>` line, which is at the _parent's_ depth) and stripping only `min(expectedIndent, thatLine'sOwnLeadingWhitespace)` — so a short-indented line like `"Field "` has all of its (whatever amount of) leading whitespace consumed as padding and none of its real characters eaten, while a normally- or over-indented line is unaffected. Verified against the real fixture directly (a harness self-test runs `upstreamSnapshot('02-molecules', 'field', 'Field Component textarea with all attributes 1')` with no fixture root override) plus new hand-authored `fixture-atoms/spacing` entries reproducing the same shape. Also added: two consecutive real _text_ lines (as opposed to tag/attribute/blank lines) are now joined with a real `\n` (which `normaliseHtml` collapses to one space) rather than concatenated directly — the shape one multi-line text node's own embedded `\n` prints as (verified live: `document.createTextNode('Hello\nWorld')` → `Hello` then `World` with the second line at literal zero indent) — not hit by any pinned fixture, but covered by a fixture + self-test anyway per the review. No `01-atoms` port needed to change: every fixture this task's 11 components exercise happens to have each text node's own indent exactly matching (or exceeding by the "real extra space" pattern) its structural depth — the under-indented case is unique to `Field`'s raw-HTML-via-string-concatenation content, not hit elsewhere yet. |
| Link | `link.twig`: `{% if is_disabled %}disabled{% endif %}` on the `<a>` | `disabled` is not a valid attribute of Astro's `AnchorHTMLAttributes` type even though upstream renders it there for a disabled link — same cast pattern as Button's link-kind branch (`{...({ disabled: ... } as Record<string, unknown>)}`), not a new decision but reused here. |
| Popover | `content`, `content_top`, `content_bottom` documented under "Slots:" but the twig body treats them as plain variables (`{% if content_top is not empty %}...{{- content_top -}}`) | Ported as `string` props rendered with `set:html`, matching Fieldset's `fields` precedent (PORTING.md row 40) rather than named Astro slots. `contentTop`/`contentBottom` are not exercised by any upstream Popover snapshot — implemented by reading `popover.twig` directly (the `ct-popover__content-top`/`-bottom` wrapper divs, gated the same way `content` is). |
| Popover | `@atoms/link/link.twig` include for the trigger, passing only `text`, `url`, `is_new_window` (NOT `is_external`, despite `trigger.is_external` being documented in the doc header's `trigger` object) | `Popover.astro`'s `<Link>` call likewise omits `isExternal` — confirmed by reading `popover.twig`'s actual `include` call, not just its doc header, since the two disagree (a genuine upstream doc/body mismatch, not a porting error). `trigger.isExternal` is still declared on the `Props` interface (matching the doc header) but is read nowhere, matching upstream's own unused-field behaviour. |
| Tag | `is_new_window_content`/`is_external_content` are each captured via an untrimmed Twig `{% set %}...{% endset %}` block whose own leading newline+indentation (from the template source, not the `{{ }}` print statement) becomes part of the captured string value | The later `{{- content_markup -}}{{- is_new_window_content -}}{{- is_external_content -}}` print statement's `-` trim markers only strip whitespace written around _that_ tag in the source, never the runtime string values being printed — so a real, unconditional single leading space precedes the visually-hidden "Opens in a new tab/window" span and the external-arrow icon whenever each is rendered, regardless of icon placement. Modelled as `{' '}` immediately before each in `Tag.astro`. Same class of bug as the icon-SVG-trailing-newline rule already in this file (row 34) — an untrimmed `{% set %}` block's own template whitespace is real output, not incidental formatting. |
| Select | `options` doc header nests a second, differently-shaped `options` array under the `optgroup` case ("Each item contains: - label, value, is_selected, is_disabled", no `type` field) | Modelled as a single `SelectOption` interface with an optional nested `options` array (no separate sub-option type) rather than two interfaces, since the nested shape is a strict subset of the top-level one (no `type` field, which is simply never read for a nested option). `Select.astro` follows the same "interleave `{i > 0 && ' '}` between mapped siblings" pattern (PORTING.md row 20) at both nesting levels (`options.map` and each optgroup's `options.map`), confirmed against the `renders optgroup with options` snapshot. |
| Table | `footer` supports three shapes in `table.twig`: a Drupal-style list of `{ attributes, cols }` rows, a legacy array-of-arrays (multiple rows, plain `<th>`, no `scope`), and a legacy flat array (one row, `<th scope="col">`); `rows`/`row.attributes`/`col.attributes`/`col.content` likewise support a richer Drupal shape beyond a plain string | Per the task brief's typed interface (`header: string[]`, `rows: string[][]`, `footer`), only the two legacy footer shapes and plain-string rows/cells are ported — the Drupal `{attributes, cols}` shape (for both `rows` and `footer`) is not reproduced. No upstream Table snapshot exercises either the Drupal shape or per-row/per-cell `attributes`; both of the ported legacy footer shapes ARE snapshot-verified (single-row via the "optional attributes" test) though the array-of-arrays multi-row branch is not, so that branch's `<th>` (no `scope`) vs `<th scope="col">` distinction was verified by reading `table.twig` directly, not a captured case. `caption`/`thead`/`tbody`/`tfoot` sibling boundaries needed explicit `{' '}` insertions (same sibling-whitespace pattern as row 20) since none of them are produced by a `{% for %}` loop themselves, only wrapped by adjacent `{% if %}` blocks with source newlines between them. |
| Video | `video.twig`: `{% for source in sources %}<source .../>{% endfor %}{{ fallback_text }}` — no `{% if %}`/trim markers between the loop and `fallback_text`, so the source newline+indent between them survives as a real space in front of the fallback text, same as the loop-sibling spacing pattern (row 20) | Modelled with an explicit `{' '}` between the mapped `<source>` list and `{fallbackText}` in `Video.astro`. `fallback_text` itself defaults to `"Your browser doesn't support HTML5 video."` via `{% if fallback_text is empty %}{% set fallback_text = "..." %}{% endif %}` — ported as `fallbackTextProp \|\| "..."`, matching Twig's `is empty` (falsy-string) semantics rather than a JS `??` (`undefined`-only) default. |
| Popover | `popover.twig` has three `{% block %}` override points (`content_top_block`, `content_block`, `content_bottom_block`) wrapping `content_top`/`content`/`content_bottom` | Per R2, blocks render inline with no override mechanism in Astro — dropped, same treatment as Layout's seven blocks (row 31). No upstream Popover snapshot relies on a Drupal sub-theme overriding one of these blocks independently of the rest. |
| Tag | `tag.twig:73` — `icon_markup` is captured via an untrimmed `{% set icon_markup %}…{% endset %}` block, so it carries BOTH a leading newline (from the template source before the `{% include %}`) and a trailing newline (the SVG asset's own, per row 34) | The initial port only handled the trailing side (needed for `icon_placement: 'before'`, where `icon_markup` prints first). The leading side matters for `icon_placement: 'after'` (the component's _default_ placement, not covered by the one upstream snapshot that exercises an icon, which uses `'before'`) — `{content}{icon-after}` needs a space between them. Fixed with the same `{' '}` pattern, verified by a direct `renderNormalised` assertion (`Sample Tag <svg`) since no upstream snapshot exercises this placement. |
| Label | `label.twig`: `{% set required_text = required_text\|default('(required)') %}` — Twig's `default` filter falls back on any falsy value (`''`, `null`), not only a missing prop | The initial port used a JS destructuring default (`requiredText = '(required)'`), which only applies when the prop is literally `undefined` — an explicit `requiredText=""` would render an empty required-marker span instead of falling back. Fixed with `requiredText: requiredTextProp` destructured separately and `const requiredText = requiredTextProp \|\| '(required)'`, matching the `tag`/`size` fallback pattern already used in this component. |
| Radio | `radio.twig:27`: `value="{{ value }}"` renders unconditionally, so an undefined `value` prop still produces a bare `value=""` attribute (Twig prints nothing for an undefined variable) | The initial port passed `value={value as any}` straight through, which Astro omits entirely when `value` is `undefined` instead of rendering `value=""`. Fixed with `value={(value ?? '') as any}`, matching `Checkbox.astro`'s existing `value ?? ''` treatment of the same upstream pattern. |
| Link | `link.twig:30`: `{%- set is_new_window = is_new_window is same as(true) -%}` narrows to a strict boolean (Twig's `is same as(true)` — only the literal boolean `true` passes, not any other truthy value) before `is_new_window` is used anywhere else in the template, including the `@base/text-icon/text-icon.twig` include | The initial port passed the raw `isNewWindow` prop straight through to both the `target` attribute and `<TextIcon>`, which would treat any truthy non-boolean value (e.g. the string `"1"`) as new-window when upstream's strict `is same as(true)` check would not. Fixed with `const isNewWindowStrict = isNewWindow === true`, used for both the `target` attribute and the `<TextIcon isNewWindow>` prop. Not exercised by any upstream snapshot (every test passes a literal `true`/`false`/omits the prop), so this is a read-the-twig fix, not a newly-failing case. |
| Select | `select.twig:50`: `<optgroup label="{{ option.label }}" ...>` renders unconditionally, so an optgroup with no `label` still produces a bare `label=""` attribute | The initial port used `label={option.label}`, which Astro omits entirely when `option.label` is `undefined`. Fixed with `label={option.label ?? ''}`. Not exercised by any upstream Select snapshot (every optgroup fixture has a `label`), so a read-the-twig fix rather than a newly-failing case. |
| Accordion | `accordion.twig`: `{% for %}` loop over `panels` produces sibling `<li>`s, plus source-newline gaps around `content_top`/`content_bottom` blocks and between each panel's header/content divs | Same loop-sibling and block-adjacency whitespace pattern as row 20/52 — `{i > 0 && ' '}` between panels, plus explicit `{' '}` between each panel's header `<div>` and content `<div>`, and around `content_top`/`content_bottom` wrapper divs. The outer `{% if panels is not empty %}` gate is on the RAW `panels` array (even one with no valid title/content panels still renders the wrapper), not on the post-filter list used for the `<li>` loop — kept as two separate checks (`(panels ?? []).length > 0` for the gate, a filtered array for the loop) rather than collapsing to one. |
| Attachment | `attachment.twig`'s `links_block` builds each file link via an untrimmed `{% set link %}...{% endset %}` capture (icon include, a `<div class="...wrapper">` with a `@atoms/link` include and an optional "changed" `@atoms/paragraph` include), then feeds the array of captured strings to `@base/item-list/item-list.twig` | `ItemList.astro`'s port only accepts an array of pre-rendered HTML _strings_, which can't hold live child components — the same "single string/root target" limitation already noted for Grid/Chip. Inlined the `<ul>`/`<li>` wrapper ItemList would produce directly (its class list is deterministic) with real `Icon`/`Link`/`Paragraph` components as children instead of raw HTML strings built by hand. |
| Attachment | `attachment.twig:84`: `symbol: file.icon ? file.icon : 'download-file'` — a self-referential ternary (same expression as both the condition and the "then" branch), intended as a fallback default per the doc header | The pinned "renders with multiple files" snapshot has NO icon at all for files without an explicit `icon`, contradicting the documented 'download-file' fallback. Verified this is a genuine upstream Twig.js engine quirk, not a misreading — installed upstream's own `node_modules` and ran the real pinned jest test (passes, confirming the snapshot is accurate), then isolated it with a throwaway probe template: `{{ file.icon ? file.icon : 'download-file' }}` evaluates to an EMPTY string when `file.icon` is undefined, while the Elvis form `{{ file.icon ?: 'download-file' }}` and `{{ file.icon\|default('download-file') }}` both correctly fall back. The specific `x ? x : y` (same variable in the condition and "then" branch) shape is what triggers it. Ported to match the verified real behaviour, not the doc comment's stated intent: `{file.icon && <Icon symbol={file.icon} .../>}`, never defaulting to `'download-file'`. |
| BasicContent | `basic-content.twig`: `is_contained = is_contained is not same as(false)` | Ported as `isContained !== false` (defaults true unless the literal boolean `false` is passed) — matches Twig's `is not same as(false)` (strict, not merely falsy). `content` is documented as a Slot upstream but every test passes it as a plain string — ported as a `string` prop with `set:html`, matching Fieldset's `fields` precedent. |
| Breadcrumb | `breadcrumb.twig` builds each rendered link/separator via an untrimmed `{% set link_item %}` capture, feeding two separate `@base/item-list/item-list.twig` includes (mobile "back" link, desktop full trail) — same "single string items target" limitation as Attachment/Grid/Chip | Both `<ul>` wrappers ItemList would produce are inlined directly (deterministic class lists) with real `Link`/`Icon` components as children. |
| TextIcon (00-base) | `text-icon.twig`: `show_full_text = icon_group_disabled or (icon_placement == 'before' and is_external == false)` — `is_external == false` is a Twig equality check, and `link.twig` passes `is_external` straight through with NO default (unlike `icon_group_disabled`/`icon_single_only`, which it does default) | **Latent bug found and fixed while porting Breadcrumb** (a 00-base atom, in scope because Breadcrumb's mobile "parent" link — icon placement 'before', `isExternal` never passed — depends on it). Verified empirically against upstream's real Twig.js engine (installed `.upstream/uikit`'s own `node_modules`, ran a throwaway probe template) that `undefined == false` evaluates to **`false`** in Twig.js, unlike PHP Twig's loose-comparison rules — so `is_external == false` is true ONLY when `is_external` is the literal boolean `false`, never when merely omitted. `TextIcon.astro`'s prior code destructured `isExternal = false` (a plain JS default), which silently normalises "omitted" and "explicitly false" to the same value BEFORE this check ran, making `!isExternal` true in both cases — wrong for the "omitted" case, where Twig's real behaviour is `showFullText = false` (grouped/last-word rendering), not `true` (plain full text). Fixed by keeping the raw `isExternalProp` (undefined when omitted) for this one check (`isExternalProp === false`, strict), while every other use of `isExternal` in the component still uses the `Boolean(isExternalProp)`-coerced value (those correspond to plain truthy checks in the twig, not equality-to-`false` checks). Re-ran the full existing `00-base`/`01-atoms` parity suite (212 cases) after the fix — no regressions, confirming no prior pinned case exercised icon-placement-'before' with an omitted `isExternal`. |
| Callout | `callout.twig`'s `links_block` builds each link via `@atoms/button` (kind 'link', type 'primary' for the first, 'secondary' for the rest) then feeds the array to `@base/item-list/item-list.twig` — same "single string items target" limitation | The `<ul>` wrapper ItemList would produce is inlined directly with real `Button` components as children, matching the Attachment/Breadcrumb precedent. No outer `{% if %}` gate on the whole component (unlike Accordion/Attachment) — `callout.twig` always renders its wrapper `<div>`, even with no content/links/title at all. |
| Tag (01-atoms) | `tag.twig`: `{{- content -}}` (a plain print, no `\|raw`) | **Fixed while porting EventCard** (a 02-molecules component, in scope because EventCard's "date" `Tag` needs to embed a whole `Datetime` component's rendered markup as `content`). Discovered that upstream's jest test harness (`jest.helpers.js`) sets `options.autoescape = false` globally for every rendered template, so EVERY plain `{{ var }}` print in EVERY pinned `.snap` fixture in this whole codebase is effectively raw/unescaped HTML already, regardless of whether the twig source has `\|raw` — this explains, retroactively, why so many "plain string, no `\|raw`" props across this port (Paragraph's `content`, Fieldset's `fields`, Popover's `content`/`content_top`/`content_bottom`, etc.) all needed `set:html`, not plain text interpolation. `Tag.astro`'s `content` was still rendering as plain `{content}` (never exercised with HTML before) — changed to `<Fragment set:html={content} />`. Re-ran the full `00-base`/`01-atoms` suite (212 cases) after the fix — no regressions, since no prior `content` value contained HTML-significant characters. |
| EventCard | `event-card.twig`'s date `@atoms/tag` include's `content` is a `@base/datetime/datetime.twig` include's captured output, and its `content_tags_block` includes `@molecules/tag-list/tag-list.twig` with exactly `{ theme, tags }` | Since `Tag`'s `content` must be a real string (not a JSX node, and `Tag.astro` has no way to accept a live JSX child as its content), `Datetime`'s small, stable markup is replicated directly with a local `datetimeMarkup()` helper (mirrors `Datetime.astro` exactly: `stripTags` + the same `— ` separator) rather than routed through the Container API — a real dependency, not a shortcut, since `Datetime`'s own output is what fidelity requires here. **Task 10 update: uses `TagList`.** The tag-list markup previously inlined here (a stand-in, pending `TagList`'s own port) is now a real `<TagList theme={theme} tags={tagList} />`, matching exactly what `event-card.twig` passes to `@molecules/tag-list/tag-list.twig`. All EventCard parity cases stayed green with no expectation changes. |
| FastFactCard | `fast-fact-card.twig` — straightforward composition of `Image`/`Paragraph`, gated `with_link`/`is_card_clickable` logic identical to EventCard's | No new decisions — same explicit-`{' '}`-between-optional-blocks pattern (row 20) between image/title/summary. First component in this task's batch to pass every parity case on the first render. |
| Field | `field.twig`'s `control` normalisation (single object vs array vs default `[{name}]`), then a per-item merge of ~13 fields against the top-level prop of the same name, with three different "propagate-if-undefined" idioms (`c.x is defined ? c.x : y`, `c.x ? c.x : y`, and `placeholder` which ALWAYS uses the outer `placeholder`, never `c.placeholder`) | Ported as a `mergeControl()` function using `??` for the `is defined` idiom (matches: only `undefined` triggers the fallback) and ` |  | `for`requiredText`(matches: any falsy value falls back, per Twig's`?:`-equivalent `c.required_text ? c.required_text : required_text`— NOT`is defined`). All 24 parity cases (23 snapshot keys + coverage) passed on first render, including the `description`/`message`"default error text" dead-code distinction (description's is unreachable — nested inside a`description`-truthy gate; message's is reachable) and the `checked`/`radio`-vs-`textfield`control-array-iteration difference (only checkbox/radio iterate every`control`item; textfield/textarea/select/default-input only ever use`control[0]`). Per-control Drupal `attributes`(e.g.`new DrupalAttribute().setAttribute('data-test','true')`) are modelled as plain extra keys directly on the control object (`{ ..., 'data-test': 'true' }`), spread via each merged control's own `rest`, matching every other atom's rest-spread convention rather than a nested `attributes` key. |
| Figure | `figure.twig` — straightforward `@atoms/image` composition + icon-then-caption `<figcaption>` | `caption` is documented under "Slots:" (not "Props:") in the doc header — per this project's autoescape convention (see the dedicated convention row below), rendered with `set:html`, not plain text, even though `figure.twig` prints it with a plain `{{ caption }}`. Otherwise closes out this task's 10-component batch with no new decisions; passed every parity case on first render. |
| Field | `field.twig`'s checkbox/radio branch builds each control via `@atoms/checkbox`/`@atoms/radio` then feeds the array to `@base/item-list/item-list.twig` (`direction: is_inline ? 'horizontal' : 'vertical'`, no `size`/`modifier_class`) — same "single string items target" limitation as Attachment/Breadcrumb/Callout/EventCard, undocumented until this fix | The `<ul>` wrapper ItemList would produce is inlined directly (deterministic class list: `ct-item-list ct-item-list--{direction} ct-item-list--regular`) with real `Checkbox`/`Radio` components as children, matching the established precedent. Was previously implemented this way but not recorded in this file — added retroactively. |
| BackToTop | `back-to-top.twig`'s `text` capture includes a `@base/icon/icon.twig` include (`up-arrow`) immediately after a visually-hidden span, with the include's own `-` trim markers producing no space between them | `Button`'s `text` prop must be a real string (it is fed straight through to `TextIcon`'s `set:html={text}` branch, since `Button` has no icon of its own here), so it can't hold a live `<Icon>` JSX child — same reasoning as EventCard's `Datetime` replication. Built directly from the `icons` map, replicating `Icon.astro`'s own `class="ct-icon"`/`aria-hidden`/`role` attribute-injection formula, rather than routed through the Container API. Not documented until this fix — added retroactively. |
| (convention) | Upstream's jest test harness (`.upstream/uikit/packages/twig/tests/jest.helpers.js:20`) sets `options.autoescape = false` for every rendered template — so EVERY plain `{{ var }}` print (no `\|raw` needed) in EVERY pinned `.snap` fixture across this whole codebase is raw, unescaped HTML already, regardless of what the twig source's own escaping would do in a real (autoescape-on) Drupal deployment | **Project-wide autoescape convention**, ruled on by the task controller: a prop documented under a twig doc header's "Slots:" section, or whose doc comment says it carries HTML (e.g. `content`, `summary`, `fields`, a Slot named `title`/`caption`/`content_top` etc.), is rendered with `set:html` — matching real upstream behaviour, not merely a convenience. A prop documented under "Props:" as a plain scalar (or as one field of an array/object item under "Props:", e.g. `panels[].title`, `links[].text`, `file.name`/`file.ext`/`file.size`) is rendered as plain text UNLESS it is itself concatenated into another prop's already-HTML-bearing captured string (e.g. Attachment's `file.name` inside `link_text`) — in which case it necessarily rides along raw too, and is NOT escaped by this port, matching the harness's real (autoescape-off) behaviour exactly. Applied here: `Figure.caption` (Slot) → `set:html`. `EventCard.title` (Slot, despite looking like plain text in `title_content`) → `set:html`. `Accordion.panel.title` (a `panels[]` Props field, not a Slot) → plain text, unchanged. `Breadcrumb.link.text` (a `links[]` Props field, not a Slot) → plain text, unchanged. `Attachment.file.name`/`file.ext`/`file.size` (plain Props fields) → plain text when used alone, but raw/unescaped when concatenated into `link_text`'s HTML string (fixed: the prior comment there wrongly claimed Twig autoescapes `{{ file.name }}`, when in fact autoescape is globally OFF in the harness that produced the pinned snapshots — so pre-escaping there was LESS faithful, not more, and was removed). |
| NextStep | `next-step.twig`'s `title_content` capture routes `title` through a live `@atoms/link/link.twig` anchor (icon `right-arrow-2`, `icon_group_disabled`/`icon_single_only` true) before feeding it to `@atoms/heading/heading.twig`'s string-only `content` prop. The task brief's Interfaces line also lists a `withBackground` prop for NextStep, but `next-step.twig` has no such prop at all (grepped the whole file for "background" — no match). | The `<h4 class="ct-heading ct-theme-{theme} ct-next-step__title">` wrapper `Heading` would produce is inlined directly (link case only) so the real `Link` component can be nested as a live JSX child instead of replicated as a markup string — same reasoning as the ItemList-inlining precedent (live children needed). The link-less branch still uses the real `Heading` component. `withBackground` is NOT declared, per the fidelity rule to declare only props the Twig reads. |
| ServiceCard | `service-card.twig`'s `links_block` builds each link via `@atoms/link/link.twig` (filtered to `link.text` truthy) then feeds the array to `@base/item-list/item-list.twig` (`direction: 'vertical'`, no `size`/`modifier_class`) | Same "single string items target" limitation as Attachment/Breadcrumb/Callout/EventCard/Field: the `<ul>` wrapper is inlined directly (deterministic class list: `ct-item-list ct-item-list--vertical ct-item-list--regular ct-service-card__links`) with real `Link` components as children. |
| Logo | `logo.twig`'s root tag (`<a>` vs `<div>`) is chosen by `url is not empty`, with identical body markup either way; `{% for logo_type, logo in logos %}` iterates in insertion order, gated by `type == 'default' ? logo_type == 'primary' : true` | Root tag picked dynamically (`RootTag = url ? 'a' : 'div'`) instead of duplicating the loop body per branch. `logoEntries` built as a fixed `[primary, secondary]` tuple array, pre-filtered by the same `type`/`logoType` gate, rather than a live `for...in` loop, since object key insertion order is guaranteed `primary` first in every upstream prop shape. |
| Map | `map.twig`'s `iframe_attributes` sets a bare `allowfullscreen` (Drupal `Attribute::setAttribute('allowfullscreen', '')`) | Passed to `Iframe` as the JSX boolean-attribute shorthand `allowfullscreen` (truthy), not `allowfullscreen=""` — Astro's own htmlBooleanAttributes handling (`astro/dist/runtime/server/render/util.js`) treats an empty-string value for a known boolean attribute as falsy and omits it entirely, so a literal `""` would silently drop the attribute Astro-side even though the harness's own `BOOLEAN_ATTRS` normaliser (in `tests/parity/harness.ts`) would have tolerated either form. |
| NavigationCard | `navigation-card.twig`'s `icon_block` prints `icon` (if any) then, on its own separate `{% if %}`, the image-as-icon `<div>` — two sibling block-level `{% if %}` statements on their own lines; likewise `is_card_clickable`'s tags `<div>` is a sibling block after `content_bottom_block`, unconditional on whether `content_bottom` itself rendered | Explicit `{' '}` text-node siblings inserted between the icon and the image-as-icon div (gated on both being present), and unconditionally before the tags div whenever `isCardClickable`, matching the literal single-space text node the normaliser preserves between adjacent Twig block tags on separate source lines — same class of fix as EventCard's/Field's inter-block spacing. |
| PublicationCard | `publication-card.twig` gates its root on `file is not empty` — Twig's `empty` test on an object checks its property count, so `file: {}` (no keys) must not render | `hasFile = Boolean(file && Object.keys(file).length > 0)`, not a plain JS truthiness check on the `file` object (which would treat `{}` as truthy and render). Same class of fix as Pagination's `items: []` gate. |
| PromoCard | `promo-card.twig`'s `content_tags_block` includes `@molecules/tag-list/tag-list.twig` with exactly `{ theme, tags }` for its `tags` array, and its own `date_block` builds a `Datetime`-shaped string fed to `Tag`'s string-only `content` prop | **Task 10 update: uses `TagList`.** The tag-list markup previously inlined here (a stand-in, pending `TagList`'s own port) is now a real `<TagList theme={theme} tags={tagList} />`, matching exactly what `promo-card.twig` passes to `@molecules/tag-list/tag-list.twig`. `date_content`/`Datetime` replicated as a raw HTML string rather than routed through the Container API — same reasoning as EventCard's `datetimeMarkup`. All PromoCard parity cases stayed green with no expectation changes. |
| Pagination | `pagination.twig` documents `current`/`total_pages`/`title` under "Slots:" in its own doc header, yet reads all three as plain scalars (`current - 1`, `current == key`, `current < total_pages`, `title` fed straight into per-item `Link.title`); `items.pages` loop uses `loop.first`/`loop.last` to place ellipsis items; the root gates on `items is not empty` (Twig's `empty` test on an array/object checks element count) | `current`/`totalPages`/`title` declared as plain-scalar props despite their upstream "Slots:" doc-header placement, since the Twig genuinely reads them as scalars, per the fidelity rule to declare props matching how they're actually used. `hasItems = Boolean(items && Object.keys(items).length > 0)`, not a plain truthiness check (`items: []` must not render). `total_pages` is never passed by any upstream test, so the ellipsis-next/next-disabled/last-disabled branches gated on it are always false here, exactly matching upstream — verified against `pagination.twig` lines 793-820. Per-page `Link.title` is `title \|\| computedTitle`, matching every tested case (title always undefined) without attempting to replicate Twig's literal-duplicate-attribute-then-HTML-parser-drops-the-second-one semantics for the untested case where both are set. |
| GroupFilter | `group-filter.twig`'s `filters_block` submit button needs a native `type="submit"` HTML attribute (via Drupal `attributes`) distinct from `Button`'s own visual `type` prop (`primary`) — `Button.astro`'s `type` prop destructure consumes that key entirely, so `...rest` can never carry a second, different `type` value through the same component invocation. Also: no `<form>` wrapper renders at all when `form_attributes` is absent (not a `<div>` wrapper either) | Replicated `Button`'s own `kind === 'button'` markup formula directly (real `TextIcon` child, same class-list formula: `ct-button ct-theme-{theme} ct-button--primary ct-button--button ct-button--small ct-group-filter__submit`) rather than reusing the shared atom, matching the "sink can't carry what's needed" precedent (e.g. EventCard's `Datetime` replication). The row/filters/submit markup is duplicated across a `withForm ? <form>...</form> : <Fragment>...</Fragment>` branch instead of a dynamic tag, since a dynamic-tag `'div'` fallback was tried first and produced an extra wrapping `<div>` upstream never has (fixed after a failing parity run). `show_selected_filters`/`selected_filters`/`selected_clear_link` are ported but exercised by no upstream test (none of `group-filter.test.js`'s 6 cases sets `show_selected_filters`) — verified by hand against `group-filter.twig` lines 147-191 (Chip-based `item-list`, gated `Button` clear-link). |
| InlineFilter | `inline-filter.twig` has no upstream `.test.js` / `__snapshots__` at all (only `.scss`/`.stories.*` files exist in `.upstream/uikit/packages/twig/components/02-molecules/inline-filter`) — unlike other untested cases, there is no snapshot to verify against; its submit button has the same native-`type="submit"`-vs-visual-`type` conflict as GroupFilter's | Ported as a real Astro slot component (`items`/`itemsEnd` are genuinely arbitrary markup per their doc comments — "Ideally a single text field" — not scalar text), ported with a smoke test asserting structure/classes rather than a snapshot, per this project's established rule for components with no upstream test (see `TextIcon.parity.test.ts`). Submit button markup replicated directly, same resolution as GroupFilter's. |
| TextIcon | `text-icon.twig`'s icon-only branch (`text` empty, icon present) renders `content_markup` as exactly `icon_markup` — `icon.twig`'s `source(...)`'d SVG _file content_, which literally ends `</svg>\n` in every icon asset | That raw trailing newline is real returned-string content, not Twig template whitespace, so `{{- icon_markup -}}`'s trim dashes (which only strip Twig-added whitespace) do not remove it — it survives into the DOM as a text node that collapses to a single space before `is_new_window_content`. `icons.ts` strips that trailing newline at generation time, so `TextIcon.astro` must add the space back explicitly for this one branch (`spaceBeforeNewWindow` now covers `text ? showFullText : Boolean(resolvedIcon)`, not only the `text` case). Forced by Social Links' "required attributes" snapshot (icon, no text, `isNewWindow: true`) — the first case anywhere in this project's ported components to combine those three. |
| TextIcon | `text-icon.twig:35-40` — the `text is not empty` branch's `{# Prepare the text #}` comment (no trim dashes) is followed by `{% set rendered_text -%}` (no leading trim dash), leaving the literal newline+indentation between them un-trimmed; every other line in the branch is a fully-trimmed `{% set %}` assignment with no output | That one un-trimmed whitespace run is the branch's only literal output before `<span class="ct-text-icon__text">`, so it collapses to a real leading space whenever `hasIconOrExternal && text` — unconditionally, not gated on icon placement, `showFullText`, or `isExternal`. Previously approximated as a Chip-local special case (conditioned on `isDismissible \|\| isMultiple`, i.e. "has an icon" — see the superseded `Chip` row above); moved here since every caller of this branch needs it, not just Chip's `kind: 'input'` markup. Re-verified against the full 433-case suite (every existing `Button`/`Link`/`Chip` snapshot using this branch) with no regressions. |
| Chip | `chip.twig`'s `kind == 'input'` branch captures `text_content` (the `TextIcon` include) via `{%- set ... -%}`/`{%- endset -%}`, fully trimmed on both sides, immediately after a plain (non-self-closing) `<input ...>` tag | Per that trim, there is never Twig-template whitespace between `<input>` and `text_content` at the Chip level — the real leading space (present in the pinned `kind: 'input'`, dismiss-icon snapshot; absent from Single Filter's no-icon radio chips) turned out to come from _inside_ `text-icon.twig` itself (see the `TextIcon` row below), not from `chip.twig`. Superseded: `Chip.astro` no longer special-cases this — the space is now emitted unconditionally by `TextIcon` whenever it applies, and Chip just renders `<input />` followed directly by `<TextIcon>` with no gap of its own. |
| TagList | `tag-list.twig` includes `@base/item-list/item-list.twig` with each tag's rendered `Tag` markup captured as a string | `ItemList.astro`'s `items` prop is typed for raw HTML strings, which a live `Tag` component can't be turned into without a nested Container render — the `<ul>`/`<li>` wrapper is reproduced directly with real `Tag` children instead (`ItemList`'s class-list formula for `direction=horizontal, size=small, noGap=false`). Each `tags` entry is merged onto `{ theme }` and spread onto `Tag`, matching `tag_data = tag is iterable ? {theme}\|merge(tag) : {theme, content: tag}`. |
| Snippet | `snippet.twig`'s `content_tags_block` includes `@molecules/tag-list/tag-list.twig` with exactly `{ theme, tags }` | Passed straight through to the now-ported `TagList`. `title`/`content_top`/`content_middle`/`content_bottom` documented as Slots upstream but exercised only with plain string props — ported as `string` props rendered with `set:html` per the autoescape convention. |
| SocialLinks | `social-links.twig` includes `@base/item-list/item-list.twig` with each item's rendered `Button` markup captured as a string | Same "live component vs. string-typed `items`" situation as `TagList` — the `<ul>`/`<li>` wrapper (`direction=horizontal, size=regular, noGap=false`) is reproduced directly with real `Button` children. Surfaced the `TextIcon` icon-only/`isNewWindow` gap documented in its own row above. |
| SubjectCard | `subject-card.twig` — no unusual constructs; a smaller sibling of `PromoCard`'s image/title/link shape (no tags, subtitle, or date) | Ported directly following `PromoCard`'s established pattern (image gating, `isTitleClick`/card-clickable logic, `title`/`imageOver` as `set:html` string props). |
| Tabs | `tabs.twig` generates `links` from `panels` when `links` is empty, then includes `@atoms/link/link.twig` for each and `@base/item-list/item-list.twig` (`noGap: true`) for the link row; each panel prints `panel.content` directly (not `\|raw`) | Generated-link fallback (`text`, `url: '#'+id+'-tab'`, `role`/`id`/`aria-controls` attributes, `linkId` for `aria-labelledby`) reproduced in a plain object map. Link row's `<ul>`/`<li>` wrapper reproduced directly with real `Link` children (same "live component" situation as `TagList`/`SocialLinks`), `ItemList`'s formula for `direction=horizontal, size=regular, noGap=true`. `panel.content` rendered with `set:html` per the autoescape convention (every pinned `.snap` fixture is generated with Twig `autoescape` off). |
| Tooltip | `tooltip.twig` — `icon`/`icon_size` default to `'information-mark'`/`'large'`; the close button needs a native `type="submit"`-free plain `<Button type="tertiary" kind="button" icon="cancel">` | Ported directly; real whitespace text nodes exist between the button/description siblings and between the description's three children (content-inner, close, arrow), matching the twig's own un-trimmed layout. |
| VideoPlayer | `video-player.twig` never spreads `attributes` onto its own root `<div>` (only ever mutates/uses it for the iframe branch); the oEmbed branch does `attributes.setAttribute('allowfullscreen', '').setAttribute('data-chromatic', 'ignore')` before including `@atoms/iframe/iframe.twig`; `transcript_content` routes through `@molecules/basic-content/basic-content.twig` | Root element declares no `...rest` spread, matching upstream. `allowfullscreen` passed as a bare boolean JSX prop (Astro renders `allowfullscreen=""` as omitted, but the shorthand boolean form as a name-only attribute, matching upstream's empty-string-valued Drupal attribute). `transcript_content` branch (unexercised by any upstream snapshot — no test sets it) ported using a live `BasicContent` component per hand-verification against `video-player.twig` lines for that block. |
| SingleFilter | `single-filter.twig` includes `@atoms/chip/chip.twig` per item (attributes captured as strings via `@base/item-list/item-list.twig`) and needs a native `type="submit"` HTML attribute on its primary submit button distinct from `Button`'s own visual `type` prop | Chip row's `<ul>`/`<li>` wrapper reproduced directly with real `Chip` children (same "live component" situation as `TagList`/`SocialLinks`/`Tabs`), `ItemList`'s formula for `direction=horizontal, size=small, noGap=false`. Submit button's native/visual `type` conflict resolved the same way as `GroupFilter`'s precedent: `Button`'s `kind === 'button'` markup formula replicated directly with a real `TextIcon` child rather than reusing the atom. Per-item `name` falls back to `single-filter-${index+1}` (multiple) or `'single-filter'` (single), matching `item.name is defined ? item.name : multiple_name`. |
| TableOfContents | `table-of-contents.twig` has no upstream `.test.js` / `__snapshots__` at all (only `.scss`/`.stories.*` files exist in `.upstream/uikit/packages/twig/components/02-molecules/table-of-contents`) | Smoke test per the task brief's rule for components with no upstream test, matching `InlineFilter`'s precedent. Two mutually exclusive branches: a static `links` list, or (when `scopeSelector` is given, `links` absent) the `data-table-of-contents-*` behaviour-hook markup that `table-of-contents.js` initialises client-side — attribute names confirmed against that JS file. `content` (automatic branch only) rendered with `set:html` per the autoescape convention. |
| TableOfContents | Review fix: `table-of-contents.twig:49-53` prints all five `data-table-of-contents-*` attributes unconditionally (an unset value prints empty, the attribute is never omitted) | `theme`/`title`/`anchorSelector`/`scopeSelector`/`position` are now each coerced with `?? ''` (matching `Tooltip.astro`'s existing `data-tooltip-position` precedent) instead of leaving `undefined` to fall through to Astro's own omit-on-undefined behaviour. This is a real-behaviour fix, not just a markup one: `table-of-contents.js:150` selects on `[data-table-of-contents-position]` existing at all, so an omitted `position` attribute previously meant the behaviour-hook would never initialise. Added a smoke test asserting all five attributes are present with no optional props given. |
| VideoPlayer | Review fix: `video-player.twig:12,40-41` declares/folds a `vertical_spacing` prop into the root class list (`ct-vertical-spacing--<value>`); `video-player.twig:74` gates the outer `.ct-video-player__transcript-block` on `transcript_link is not empty` (any non-empty object), separately from the inner `.ct-video-player__links` row's own `transcript_link.url is not empty` gate | Added the missing `verticalSpacing` prop/class fold, tested directly (no upstream snapshot sets it). Split the single `hasTranscriptLink` boolean this port previously used for both gates into `hasTranscriptLinkObject` (outer block) and `hasTranscriptLinkUrl` (inner links row) — a `transcriptLink` object without a `url` (e.g. `{ text: '...' }` alone) now still opens the transcript block, matching upstream; tested directly (unexercised by any upstream snapshot). |
| SingleFilter | Review fix: `single-filter.twig`'s per-item `{% set filter_item %}...{% endset %}` capture is un-trimmed, so it is whitespace (not truly empty) even when `item.text` is empty — and `item-list.twig:27-32`'s `item is not empty` check (Twig's `empty` test on a string is only true for the literal `''`) does not skip a whitespace-only item | This port previously filtered text-less items out of the array entirely (`items.filter((item) => item.text)`), omitting their `<li>` outright. Now every item gets its own `<li class="ct-item-list__item">` (matching upstream 1:1), with the `Chip` rendered inside only when `text` is truthy — an empty `<li>` otherwise. Tested directly (unexercised by any upstream snapshot). The `withForm`/no-form branches still duplicate this same items-list body (an existing, pre-Task-10 structural duplication inherited from the Twig's own `{% if form_attributes %}<form>...{% endif %}` shape) — noted here, no refactor made. |
| Alert | `alert.twig` documents `title`/`description` as Slots but every upstream test passes plain strings; the dismiss button needs `create_attribute({...}).setAttribute(...)` merged onto `@atoms/button/button.twig`'s own `attributes` | Ported as `string` props rendered with `set:html`, per the autoescape convention. Dismiss button rendered as a real `Button` with `data-alert-dismiss-trigger`/`id`/`title` passed as plain props (rest-spread), matching `create_attribute({...})`'s effect. `data-alert-id`/`data-alert-type` — the `alert.js` behaviour hook's identifiers — always present (`id ?? ''`). |
| Message | `message.twig` documents `title`/`content` as Slots but every upstream test passes plain strings; `content` include is `@atoms/paragraph/paragraph.twig` with `{ theme, content, modifier_class: 'ct-message__content' }`; `has_aria` (default `true`) fully omits `role`/`aria-label`/`aria-live` when `false` | Ported `title`/`content` as `string` props with `set:html`; `content` rendered via a real `Paragraph`. `hasAria` spreads `{ role, 'aria-label': type, 'aria-live': 'assertive' }` conditionally via `{...(hasAria ? {...} : {})}` rather than three separate `?? undefined` attrs, since upstream fully omits all three together, not independently. |
| Campaign | `campaign.twig`'s `links_block` per-link `{% include button.twig %}` markup is captured as a string then passed to `@base/item-list/item-list.twig`; both image-column `{% if image_position == 'right' %}...{% else %}...{% endif %}` branches on the first (`content`) column are identical text, but differ on the second (`image`) column | Links rendered directly as real `Button` children inside the `ItemList` class-list formula (`ct-item-list ct-item-list--horizontal ct-item-list--regular ct-campaign__links ct-item-list--horizontal-middle`), matching `TagList`/`SocialLinks`'s precedent — first link `type="primary"`, rest `"secondary"`. Second column's class list built with `class:list` since only it varies by `imagePosition`. |
| List | `list.twig`'s `empty_block` gates on `empty is defined and (rows is not defined or rows is empty)`; `rows_above`/`rows_below` are documented `[array]` but every upstream test passes a plain string, coerced to `[value]` by `rows_above is not iterable` before an un-trimmed-once (`{%- ... -%}`) concatenating loop | `empty`'s "is defined" half ported as `typeof empty !== 'undefined'` (every prop key is always "defined" once destructured in Astro, so this is the closest faithful translation — matches every upstream test case). `rowsAbove`/`rowsBelow` typed `string \| string[]`, normalised to an array and joined with no separator (matching the loop's full trim). |
| MobileNavigationMenu | `mobile-navigation-menu.twig`'s top-level `menu_links` macro is "copied as-is from `@base/menu/menu.twig`"; its `menu_links_below` override replaces `Menu.astro`'s collapsible-panel below-rendering with an "open submenu" trigger button plus a nested `data-flyout` panel (own `MobileNavigationClose`, a "back" button, then the same macro recursed at `menu_level + 1`) | Ported as a self-recursive component distinct from `Menu.astro` (the below-rendering genuinely differs, not just a prop). No upstream snapshot in `mobile-navigation.test.js.snap` exercises an item with `below` — every test's `top_menu`/`bottom_menu` items are flat — so this branch has only a structural smoke test (see `MobileNavigationMenu.parity.test.ts`), not a pinned-snapshot one. `MobileNavigationTrigger`/`MobileNavigationClose` likewise have no upstream `.test.js` at all — smoke-tested per `InlineFilter`'s precedent, and exercised indirectly (as `MobileNavigationClose`) inside `MobileNavigation`'s own real snapshot cases. |
| Banner | `banner.twig`'s `title_block`/`content_middle_block`/`content_block`/`breadcrumb_block` each independently widen their column from `col-xxs-12` to `col-xxs-12 col-m-6` whenever `featured_image` is present — four separate `{% if featured_image is not empty %}` guards, not one shared class | `hasFeaturedImage ? 'col-xxs-12 col-m-6' : 'col-xxs-12'` computed once (`contentColClass`) and reused at the three body sites that share its exact wording (`title`/`contentMiddle`/`content`), with the breadcrumb site's near-identical guard kept inline since it is gated by a different condition pair. `breadcrumb.twig` included with its already-ported real component. |
| Header | `header.twig`'s `content_top2_block`/`content_top3_block` have no individual `{% if %}` guard of their own — both cols render unconditionally whenever EITHER `content_top2` or `content_top3` is non-empty (only the shared outer `.container` is gated on the pair); `content_middle2_block`/`content_middle3_block` DO gate individually — an asymmetry in the upstream source, not a typo | Reproduced faithfully: `hasTop2Or3` gates the shared container (both `content-top2`/`content-top3` `<div>`s always render inside it once open), while `contentMiddle2`/`contentMiddle3` each gate their own `<div>` independently. Not directly exercised by any upstream snapshot (no test sets exactly one of `content_top2`/`content_top3`), but this is what the Twig literally does. |
| Footer | `footer.twig` declares 5 middle slots (`content_middle1`..`5`), not 4 as summarised in the task brief — `content_middle5` spans the full `col-xxs-12` (the other four are `col-m-3` quarters); every one of its 9 slots (unlike `Header`'s top2/top3 pair) gates its own `<div>` individually | Ported all 5 middle slots for fidelity to the actual upstream Twig (verified against `footer.test.js`, which does exercise `content_middle5`), rather than the brief's 4. |
| Footer | Review fix: `footer.twig:40`'s root `<footer>` gate's `is not empty` chain deliberately excludes `content_middle5` (only `content_top1/2`, `content_middle1..4`, `content_bottom1/2` are listed), while the inner `.ct-footer__middle` container's own gate (`footer.twig:70`) includes all five | `shouldRender` now excludes `contentMiddle5`; `hasMiddle` (inner container) still includes it — a Footer given ONLY `contentMiddle5` now renders nothing, matching upstream. Tested directly (unexercised by any upstream snapshot): "only `contentMiddle5`" renders nothing, "`contentMiddle1` only" renders. |
| Banner | Review fix: `banner.twig:78,84` — `breadcrumb is not empty` / `featured_image is not empty` are Twig's `empty` test on an OBJECT (property-count check), not on `.links.length`/`.url` specifically — an object with any key at all (even `{ activeIsLink: true }` with no `links`, or `{ alt: '...' }` with no `url`) counts as non-empty | `hasBreadcrumb`/`hasFeaturedImage` now use `Object.keys(x).length > 0` instead of `.links.length`/`.url`. `Breadcrumb`'s own `links.length` gate and `Image`'s own `url` gate still separately govern what renders _inside_ the opened wrapper. All 4 Banner snapshots still green; two new direct tests cover the previously-unexercised "object present but missing the expected key" cases. |
| List | Review fix: `list.twig:139,163` — `{% if rows_above -%}` / `{% if rows_below %}` gate on the RAW `rows_above`/`rows_below` prop's own truthiness (an array is Twig-truthy whenever non-empty), evaluated _before_ the `is not iterable` coercion — not on the joined HTML string produced after coercion | Added `hasRowsAbove`/`hasRowsBelow` (`Array.isArray(x) ? x.length > 0 : Boolean(x)`) as the actual gates, keeping `rowsAboveHtml`/`rowsBelowHtml` only for the joined content itself. A non-empty array of all-empty-string rows (joined content `''`) now still opens the container, matching upstream. Tested directly (unexercised by any upstream snapshot). |
| MobileNavigationMenu | Review fix: the level-0 `<ul>` carried a `...rest` spread, but `mobile-navigation-menu.twig`'s only caller — `mobile-navigation.twig`'s two `{% include ... with { theme, items, modifier_class } only %}` calls — passes `only`, stripping every other variable (including `attributes`) from the included template's context, so the macro's own `{{- attributes | default('') -}}`(reachable only via`{% import _self %}` context-sharing) can never actually receive anything at this call site | Dropped the `...rest` capture and spread entirely (nothing for it to ever carry in practice) and removed the now-unused `[key: string]: unknown` index signature; documented the `only`-keyword reasoning in the component's own doc comment rather than leaving a silently-dead spread. |
| Navigation | `navigation.twig`'s `type in ['dropdown', 'drawer']` branch decorates each top-level item with `data-collapsible`/`data-collapsible-collapsed`/`data-collapsible-icon-group`/`data-collapsible-group`/`data-collapsible-duration` attributes and an `ct-navigation__has-dropdown[ ct-navigation__dropdown-columns--N][ ct-navigation__dropdown-columns--fill]` class via `items[key]\|merge(...)`, entirely independent of `@base/menu/menu.twig`'s own `is_collapsible` prop (which produces a different shape: an extra `.ct-menu__item__link-trigger` button, `aria-expanded`, `data-collapsible-panel`, driven by active-trail/expanded state) — the `none`/`inline` branch instead strips `below` from every top-level item so no submenu ever renders | Reused `Menu.astro` for recursive item/link rendering, called with `isCollapsible` left at its default `false`; pre-computed the decorated/stripped item array in frontmatter (mirroring the twig `merge()` calls) and passed it as `items`. `menuLevelModifierClass={{1: 'container'}}` only for `type: 'drawer'`. Only top-level items are touched, matching `items\|keys` (twig never recurses into `below`). All 5 snapshots green, including the submenu/drawer case with nested `ct-menu__sub-menu`. |
| Slide | Review fix: `slide.twig`'s own `.test.js`/`__snapshots__/slide.test.js.snap` live inside the SAME upstream directory as `slider.twig`/`slider.test.js` (`03-organisms/slider/`, not a `slide/` directory of its own) | The harness's `SnapshotMeta` gained an optional `dir` field (directory segment when it differs from `name`); `snapshotFile` now reads `meta.dir ?? meta.name` for the directory and `meta.name` for the `.snap` filename stem, threaded through `upstreamSnapshot`/`listSnapshotKeys`/`missingSnapshotKeys`/`expectAllKeysCovered`/`parityCase` (all now take one `meta` object; `upstreamSnapshot`/`listSnapshotKeys` changed from positional `(layer, name, ...)` args). `Slide.parity.test.ts` now uses `parityCase({ layer: '03-organisms', name: 'slide', dir: 'slider' }, key, Slide, ...)` for all 4 real upstream snapshot keys, replacing the earlier hand-copied literal-string comparisons (confirmed byte-identical to the real oracle by the prior review). `image`/`tags`/`date`/`dateEnd`/`links`/`contentTop`/`contentBottom` remain unexercised by any upstream snapshot (the real `.snap` only varies `heading_level`); each is a direct translation of `slide.twig`, kept verified by the same 9 direct assertions as before. `image is not empty` uses the same object-presence (`Object.keys().length > 0`) semantics as the Banner/Promo review fixes, not `.url` truthiness. `links_block`'s item-list is inlined with real `Button` children (same "single string items target" limitation as Attachment/Breadcrumb/Callout/EventCard/Field). `date_block`'s `Tag` content is a hand-built `Datetime`-equivalent markup string, mirroring `EventCard.astro`'s own precedent (`Tag`'s `content` is a string prop, not a JSX slot) — this `stripTags` helper is now duplicated across `Datetime.astro`/`EventCard.astro`/`PromoCard.astro`/`Slide.astro`; left as-is (each copy is a one-line pure function with no shared import path across layers) but noted here per review. |
| Navigation | Review fix: `navigation.twig:44,72`'s `{% if items[key].below %}` is false for an empty array too (Twig arrays are falsy when empty) — `Navigation.astro` was gating on mere presence (`!item.below`), so an item with `below: []` was incorrectly decorated with `data-collapsible*`/`ct-navigation__has-dropdown` for `type: 'dropdown' | 'drawer'` | Changed the gate to `(item.below?.length ?? 0) === 0` (return the item untouched) — an item with `below: []` now gets neither the collapsible decoration nor an active `below`-strip (already a no-op either way), matching upstream. Tested directly (unexercised by any upstream snapshot): dropdown and drawer types with `below: []` produce neither `data-collapsible` nor `ct-navigation__has-dropdown`. Also added a direct test for `type: 'dropdown'` with a real, populated `below` (no upstream snapshot exercises dropdown-type with an actual submenu — only drawer's does) — confirms `dropdownModifierClass`'s non-drawer branch (`ct-navigation__has-dropdown` only, no `dropdown-columns`/`fill` class, no `container` level-1 class). |
| Promo | Minor: `promo.twig`'s `{% if link %}` is a plain Twig truthy test on an object, which — like the `is not empty` review fixes already in this file — is a property-count check, not "any object is truthy": `link: {}` is falsy, same as `null`/absent | Added `hasLink = Object.keys(link ?? {}).length > 0` as the actual gate (was `link &&`, which treats `{}` as truthy in JS); `link?.text`/`url`/etc. inside the opened block reflect that `link` no longer narrows from the gate itself. Tested directly (unexercised by any upstream snapshot): `link: {}` renders no `.ct-promo__links`. |
| Slider | `slider.twig`'s progress-indicator `<Tag>` hardcodes `content: 'Slide 1'` regardless of actual slide count — `slider.js`'s vendored runtime behaviour rewrites it via `data-slider-progress` | Ported as the literal string `'Slide 1'`, matching the twig source exactly (not a bug — the static markup is a placeholder the vendored JS updates at runtime). |
| Behaviours init | Task 13's brief assumed the vendored behaviours self-init on `DOMContentLoaded` and asked for an `astro:page-load` re-dispatch. Checked every file in `src/civictheme/js/behaviours/*.js` (and the matching upstream sources in `.upstream/uikit`): none register a `DOMContentLoaded` listener — each self-initialises directly at the top level of its module, once, at module evaluation (upstream's own behaviour). Review found the re-dispatch was a dead no-op (nothing listens for it) and, more importantly, a real re-init is impossible without editing vendored files: the constructors (`CivicThemeCollapsible`, `CivicThemeTabs`, etc.) are module-private and never exported, so nothing outside `./behaviours/*.js` can re-invoke them; and `tabs.js`/`skip-to-target.js` have no double-init guard, so re-running the top-level `querySelectorAll` init a second time would attach duplicate event listeners. No behaviour references `Drupal`/`drupalSettings`/`once()`. | Controller ruling: this template deliberately does not enable `<ClientRouter>` (view transitions) — every navigation is a full page load, so the behaviours simply re-run naturally with no swap to react to. `civictheme.js` was reduced to a single `import './behaviours/index.js';` plus a header comment recording this decision and its reasoning (the missing exports/guards above). If view transitions are ever wanted, the vendoring step would need an upstream change that exports initialisers and adds the missing guards, before any re-init hook is added here. |
| Playwright preview | `astro preview` (Astro 7) auto-detects an "AI agent environment" (via the `am-i-vibing` package) and, when detected, always daemonises itself in the background and exits the launching process immediately — even without `--background`. Playwright's `webServer` requires its `command` to stay in the foreground; the daemonised exit was reported as `Process from config.webServer exited early.` | `playwright.config.ts`'s `webServer.env` sets `ASTRO_PREVIEW_BACKGROUND: '1'`, which Astro's CLI checks (`!process.env.ASTRO_PREVIEW_BACKGROUND && isRunByAgent()`) to skip the agent auto-detection and run `astro preview` in the foreground. |
| List | Task 14b (review fix): `list.twig`'s doc header documents `title`/`content`/`filters`/`results_count`/`rows`/`empty`/`pagination`/`footer` as **Slots**, but the original port typed all eight as plain `string` props only — forcing any consumer needing live Astro children (e.g. `ListingGrid`'s cards) to inline `List`'s own wrapper markup instead of composing `<List>` directly. | Each of the eight now also has a same-named Astro slot (`Astro.slots.has('x')`) that TAKES PRECEDENCE over the string prop when present; every wrapper `{% if %}` gate widened to `Boolean(prop) \|\| Astro.slots.has('prop')` so a slot-only usage still opens its wrapper. Every existing string-prop parity case is untouched (the string-prop branch is byte-identical to before) and passes; new tests assert slot-vs-string-prop parity for `rows`/`filters`/`pagination`/`resultsCount`/`footer`/`title`/`content`/`empty`. `title`/`content`/`empty` route the string-prop branch through `Heading`/`Paragraph` (which add their own wrapper markup), so their slot-parity tests compare against that sub-component's own known output rather than the raw string — a slot is expected to supply already-formatted markup, not raw text needing that wrapper. |
| Header | Task 15: `header.twig`'s doc header documents `content_top1..3`/`content_middle1..3`/`content_bottom1` as **Slots**, but the original port typed all seven as plain `string` props only — a layout composing a live `<Logo>`/`<Navigation>`/`<Search>` into the header could not do so without pre-rendering HTML strings. | All seven now also have a same-named Astro slot (`Astro.slots.has('x')`) that TAKES PRECEDENCE over the string prop when present; every wrapper `{% if %}` gate widened to `Boolean(prop) \|\| Astro.slots.has('prop')` so a slot-only usage still opens its wrapper. Every existing string-prop parity case is untouched and passes; new tests assert slot-vs-string-prop parity for all seven. The ROOT `shouldRender` gate is widened the same way as every inner wrapper gate — it is now `hasTop |  | hasMiddle |  | hasBottom1`, each term itself `Boolean(prop) |  | Astro.slots.has('prop')`— so a header built only from slots still renders its`<header>`element. The upstream asymmetry those gates encode survives intact:`contentTop2`/`contentTop3`still share one gate (both cols render whenever either is present, slot or string), while`contentMiddle2`/`contentMiddle3`still gate individually.`SiteHeader.astro`composes`<Header>` with live children through these slots. |
| Footer | Task 15: `footer.twig`'s doc header documents `content_top1..2`/`content_middle1..5`/`content_bottom1..2` as **Slots**, but the original port typed all nine as plain `string` props only. | All nine now also have a same-named Astro slot with the same precedence and widened-gate rule as `List`/`Header`, the ROOT gate included: `shouldRender` is now a disjunction of the widened `hasContent*` Booleans, so a footer built only from slots still renders. `contentMiddle5` keeps its upstream asymmetry: it stays deliberately absent from `shouldRender` (footer.twig:40) while present in the inner `.ct-footer__middle` gate (footer.twig:70), so a `contentMiddle5` slot alone still does not open the root `<footer>`, exactly as the string prop does not. New tests assert slot-vs-string-prop parity for all nine. |
| Banner | Task 15: `banner.twig`'s doc header documents `content_top1..3`/`site_section`/`title`/`content_middle`/`content`/`content_below`/`content_bottom` as **Slots**, but the original port typed all nine as plain `string` props only. | All nine now also have a same-named Astro slot with the same precedence and widened-gate rule, the ROOT gate included: `shouldRender` is now a disjunction of the widened `has*` Booleans, so a banner built only from slots still renders. `breadcrumb`/`featuredImage` stay object props, so their own property-count gates are untouched. `title`/`siteSection` route the string-prop branch through `Heading`, so their slot-parity tests compare against `Heading`'s own known output rather than the raw string — a slot supplies already-formatted markup. `PageBanner.astro` composes `<Banner>` through these slots, and `ListingGrid.astro` composes `<List>`'s `pagination` slot the same way. Caveat for consumers: Astro registers a named slot at compile time, so `Astro.slots.has('x')` is true even when the expression wrapping a `<Fragment slot="x">` evaluates to false — a conditional slot therefore still opens its (empty) wrapper. Compose two branches sharing one spread props object instead (see `PageBanner.astro`). |
| Grid | Task 14b (review fix): `grid.twig` has no upstream Slots section (`items` is documented as a plain `[array]` prop, each entry pre-rendered content), but the original port likewise accepted only pre-rendered HTML strings per item, forcing `ListingGrid` to build its own column markup by hand instead of using `Grid`. | Added a default `<slot />`, gated by `Astro.slots.has('default')`, that TAKES PRECEDENCE over `items` — the row/container wrapper markup is unchanged, but its content is the slot's children (already-wrapped columns) instead of the `items` map. `items.length > 0` in the render gate widened to `items.length > 0 \|\| hasChildren`. Every existing `items`-based parity case is untouched; new tests assert that a single item passed as pre-wrapped slot markup (`<div class="col">…</div>`) renders identically to the same content passed via `items`, both with and without `useContainer`. |
| ListingGrid | Task 14b (review fix): the original `ListingGrid` inlined `List`'s header/rows wrapper markup and hand-built `Grid`'s row/column markup directly (with a comment explaining that neither component accepted a `<slot />` at the time) instead of composing the two organisms — a composition violation Task 15 (layouts) would otherwise have repeated. | Now composes `<List title={title} theme={theme} class="ct-listing-grid">` with the cards passed as `<Fragment slot="rows">` (containing a `<div class="container">` wrapping `<Grid useContainer={false}>` with the live card components as children), rather than hand-inlining either component's markup. `Grid` is used with `useContainer={false}` (and the `.container` div built here instead) specifically so the rendered rows subtree doesn't also pick up `Grid`'s own `aria-live="polite"` container attribute. This composition is NOT byte-identical to the pre-Task-14b inlined markup: `List`'s header block unconditionally renders a second (here-empty) `col-xxs-12 col-m-3` link-above column whenever `title` is set (upstream's own header markup, previously hand-omitted since `ListingGrid` never renders `linkAbove`), and `List`'s `rows` slot wrapper (`<div class="ct-list__rows">`) adds one extra nesting level around the `.container`/`.row` cards markup (previously merged onto a single `<div class="container ct-list__rows">`). Both deltas are semantically inert (an empty column div; an extra wrapper div with no layout-affecting styles of its own) and were captured/diffed against `dist/events.html` before and after this change — see the Task 14b report for the exact diff. Accepted as the cost of composing through `List`/`Grid`'s real slot mechanism instead of hand-inlining their markup. |
| Fieldset / Popover / Accordion / Callout / BasicContent / Figure / Attachment | Task 14c: each ports at least one Slot-documented prop (`Fieldset.fields`; `Popover.content`/`contentTop`/`contentBottom`; `Accordion.contentTop`/`contentBottom`; `Callout.title`/`content`/`contentTop`/`contentBottom`; `BasicContent.content`; `Figure.caption`; `Attachment.contentTop`/`contentBottom`) that the twig body renders as a plain HTML-bearing variable via `set:html` — a slot can stand in for each cleanly. | Slots added with string fallback: each prop now also has a same-named Astro slot (`Astro.slots.has('x')`) that TAKES PRECEDENCE over the string prop when present; every wrapper `{% if %}` gate (root gate included, for `Popover`/`BasicContent`) widens to `Boolean(prop) \|\| Astro.slots.has('prop')`. `Callout.title`/`content` route the string-prop branch through `Heading`/`Paragraph` (same treatment as `List`), so their slot-parity tests compare against those sub-components' own known output. Every existing string-prop parity case is untouched and passes; new tests assert slot-vs-string-prop parity for every prop listed, plus a "slot alone still opens its wrapper" case per component. |
| EventCard / Map / GroupFilter / PromoCard / NavigationCard / NextStep / SingleFilter / PublicationCard | Task 14c: each ports at least one Slot-documented prop rendered as a plain HTML-bearing variable via `set:html` (or, for `title`/`content`/`summary`/`location`, routed through a sub-component like `Heading`/`Link`/`Paragraph`) — a slot can stand in for each cleanly. | Slots added with string fallback, same TAKES-PRECEDENCE/widened-gate convention as the earlier batch. `EventCard`/`PromoCard`: `imageOver`/`contentTop`/`title`/`contentMiddle`/`location` (EventCard only)/`summary`/`contentBottom` — root gate widens via `title`; the `--image-over` modifier class now checks `hasImageOver` (slot OR string), not just the string. `NavigationCard`: same set minus `location`, plus `icon` explicitly NOT slotted (fed into a sibling `Icon`'s `symbol` prop — a data value, not markup). `NextStep`: `title`/`content`/`contentTop`/`contentBottom`, root gate via `title`. `Map`/`GroupFilter`: `contentTop`/`contentBottom` only (their only upstream Slots). `PublicationCard`: `imageOver`/`contentTop`/`contentMiddle`/`summary`/`contentBottom` (`title` stays a plain prop — documented under upstream's "Props:", not "Slots:"). `SingleFilter`: `contentTop`/`contentBottom`/`title`/`formHiddenFields` slotted; `submitText`/`resetText` NOT slotted (fed into `Button`'s `text` prop, the same "sink can't carry what's needed" case as `Tag.content`/`Button.text`); `formAttributes` NOT slotted (a Drupal Attribute object spread onto `<form>`, and its presence alone decides whether a `<form>` wraps the row); `items` NOT slotted (structured per-item data driving live `Chip` children, not a single HTML value). Every existing string-prop parity case is untouched and passes; new tests assert slot-vs-string-prop parity for every slotted prop (comparing against the sub-component's own known markup where one is routed through `Heading`/`Link`/`Paragraph`), plus a "slot alone still opens its wrapper" case per component. |
| ServiceCard / TableOfContents / Snippet / Search / SubjectCard / Tooltip / TagList / Pagination / InlineFilter | Task 14c: sweep of the remaining Slot-documented molecules — most render their Slot props as plain HTML-bearing variables via `set:html` (or through a sub-component), a few consume them as data or attribute values that a slot cannot stand in for. | `ServiceCard`: `contentTop`/`title`/`contentBottom` slotted (root gate widens via `title`; `title` bypasses `Heading` when slotted). `TableOfContents`: `content` slotted (only used inside the already-unconditional behaviour-hook `<div>`, so no gate to widen). `Snippet`: `contentTop`/`contentMiddle`/`contentBottom`/`title`/`summary` slotted (root gate widens via `title`; `summary` bypasses `Paragraph`); `tags` NOT slotted — fed as structured array data into `TagList`'s `tags` prop, not a single HTML value. `SubjectCard`: `title`/`imageOver` slotted (root gate widens via `title`). `Tooltip`: `content` slotted (root gate widens); `title` NOT slotted — it feeds two plain HTML attributes (`aria-label`, `title`), not markup. `TagList`: `contentTop`/`contentBottom` slotted. `Search`: NEITHER `text` nor `url` slotted — `text` is wrapped in a `<span>` and fed into `Link`'s `text` prop (the `Tag.content`/`Button.text` "sink can't carry what's needed" case), `url` is consumed as `Link`'s href attribute value. `Pagination`: already recorded above (Task-agnostic) as reading its `current`/`totalPages`/`title` "Slots" as plain scalars, not HTML — no slot added, no change needed. `InlineFilter`: already fully slot-based from its original port (no upstream test to preserve string-prop parity against) — no change needed. Every existing string-prop parity case is untouched and passes; new tests assert slot-vs-string-prop parity for every slotted prop, plus a "slot alone still opens its wrapper" case per component. |
| Alert / Campaign / Message / Navigation / MobileNavigation / Slide / Slider / SideNavigation / Promo / Webform | Task 14c: the last layer of Slot-documented organisms — each renders its Slot props as plain HTML-bearing variables via `set:html` (or through a sub-component like `Heading`/`Paragraph`). | Slots added with string fallback, same TAKES-PRECEDENCE/widened-gate convention as the earlier batches. `Alert`: `title`/`description` slotted, root gate widens via `description`. `Campaign`: `contentTop`/`title`/`content`/`contentBottom` slotted (no root gate to widen — the root always renders). `Message`: `title`/`content` slotted, root gate widens to `hasTitle |  | hasContent`. `Navigation`/`SideNavigation`: `title` slotted (bypasses `Heading`); root item-count gate is unrelated to `title` and stays untouched. `MobileNavigation`: `contentTop`/`contentBottom` slotted. `Slide`: `contentTop`/`title`/`content`/`contentBottom` slotted (no root gate — always renders). `Slider`: `contentTop`/`title`/`contentBottom` slotted; `slides` NOT slotted (a plain "Props:" entry, not documented as a Slot upstream) — root gate stays `slides &&`, unrelated to the three Slots, and the root `aria-label` reads the `title` slot's rendered text (tags stripped, escaped entities decoded) when the slot is present, else the `title` prop, else `'Slider'` — the slot is rendered once with `Astro.slots.render` and printed via `set:html`, so the heading markup is unchanged. `Promo`: `contentTop`/`title`/`content`/`contentBottom` slotted (root gate widens via `title`/`content`; `contentTop`/`contentBottom` stay nested inside that same outer gate, matching upstream — a `contentTop` slot alone, with neither `title` nor `content`, still renders nothing). `Webform`: `referencedWebform` slotted, root gate widens. Every existing string-prop parity case is untouched and passes; new tests assert slot-vs-string-prop parity for every slotted prop, plus a "slot alone still opens its wrapper" case per component (adjusted to the real gate structure where a nested slot's wrapper depends on an outer, unrelated gate, e.g. `Promo`). |
| Alert (dismiss) | `alert.twig` renders the close button unconditionally and has no `dismissible` prop; `alert.js` attaches its dismiss listeners only from `insert()` (alert.js:212), i.e. only to alerts fetched from `data-alert-endpoint`, and initialises solely on `[data-component-name="ct-alerts"]` (alert.js:266) — a wrapper the twig never renders (it is Drupal-side glue). | `Alert` gains a NON-upstream `dismissible?: boolean` prop defaulting to `true`, so the default render stays byte-identical (parity, 697 cases, unchanged) while an alert authored with `dismissible: false` omits the button. `SiteAlerts.astro` supplies the missing `[data-component-name="ct-alerts"]` wrapper (no `data-alert-endpoint`, so the behaviour makes no request against build-time alerts), and the dismiss listener lives in the shim file `src/civictheme/js/civictheme.js` rather than in the vendored behaviour: it mirrors `dismiss()`/`setCookieValue()` exactly (remove the `[data-component-name="ct-alert"]` element, record `data-alert-id` against a hash of its HTML-stripped markup in the `ct-alert-hide` cookie) and re-applies that cookie on load, the static equivalent of `filter()`'s `hasCookieValue` skip. Covered by `tests/e2e/behaviours.spec.ts` ("site alert dismisses on click"). |
| Heading (content) | `heading.twig`'s doc header documents `content` under **Props**, not Slots, and Drupal renders it with autoescape ON — so HTML passed as `content` is printed as text. | RULING (closes the "Slot-documented titles escaped" question): `Heading` keeps rendering `content` as TEXT. No slot, no `set:html`. A caller needing live markup in a heading bypasses `Heading` and inlines the `<hN class="ct-heading …">` wrapper — the precedent `NextStep` already sets for its linked title. Every consumer that routes a slot through a title (List, Banner, Callout, Snippet, ServiceCard, SubjectCard, Navigation, …) already bypasses `Heading` on the slot branch, so nothing regresses. |
| Search | `search.twig` documents `text`/`url` as Slots but consumes both as scalars: `text` is wrapped in a `<span>` and fed to `@atoms/link`'s `text` prop, `url` is the link's `href` attribute value. | Neither is slotted (the `Tag.content`/`Button.text` "sink cannot carry markup" case). Separately, the template's `/search` ROUTE is a native Quant AI Search page (`src/pages/search.astro`, `src/lib/quant-search*.ts`): it keeps the CivicTheme search form (`action="/search"`, GET) and renders the API's results as `Snippet`s in a `List` — see the README's "Search" section. |
<!-- prettier-ignore-end -->

## Story parity

Task 15b compares every ported component against the args and markup of the
upstream Storybook stories (`tests/story-parity/`, oracle in
`tests/story-parity/fixtures/`, captured by
`scripts/capture-upstream-stories.mjs`). Run it with `npm run test:story-parity`.

### Fixes the stories exposed

<!-- prettier-ignore-start -->
| Component | Story | Gap |
| --- | --- | --- |
| Layout | `base-layout--layout` (and its five sidebar variants) | Every slot was slot-only, so a story passing `content`/`content_top`/`sidebar_*` as HTML strings rendered nothing at all. Added the string props with the project's TAKES-PRECEDENCE slot convention. |
| Fieldset | `atoms-form-controls-fieldset--fieldset` | No inter-sibling whitespace inside `ct-fieldset__wrapper`: `description`, `message`, `fields` and `suffix` each need the separator that `fieldset.twig`'s blank lines produce. |
| Datetime, EventCard, PromoCard, Slide | `molecules-list-event-card--event-card`, `molecules-list-promo-card--promo-card` | `datetime.twig` uses `start_iso\|default(start)`, and Twig's `default` filter falls back on an EMPTY value, not only on an undefined one. The port used `??`, which keeps `''`, so `date_iso: ''` produced `datetime=""`. Now `\|\|`. |
| VideoPlayer | `molecules-video-player--sources` (dark) | `video-player.twig:49-55` includes `@atoms/video` with `only` and does NOT forward `theme`, so the inner `<video>` is always `ct-theme-light`. The port forwarded `theme`. |
| VideoPlayer | `molecules-video-player--transcript-block` | Missing whitespace between the two transcript-toggle `<span>`s and between the toggle button and `ct-video-player__transcript-panel`. |
| TableOfContents | `molecules-table-of-contents--table-of-contents` | Missing whitespace between the `<h2>` title and the `<ul>` of links. |
| InlineFilter | `molecules-list-inline-filter--inline-filter` | `items`/`items_end` were slot-only (rendered nothing for a string arg), and whitespace was missing after the title and before `ct-inline-filter__actions`. |
| GroupFilter | `molecules-list-group-filter--group-filter-with-selected-filters` | Missing whitespace before `ct-group-filter__selected-filter-controls`, after `ct-group-filter__selected-title`, and before `ct-group-filter__selected-clear`. |
| Textarea | `atoms-form-controls-textarea--textarea` (visual) | Rendering `value` as a JSX child put the surrounding newline and indentation INSIDE the `<textarea>`. A `<textarea>`'s content is whitespace-sensitive, so that whitespace counts as a VALUE and it suppressed the placeholder — the field rendered blank where upstream shows "Placeholder". `normaliseHtml` collapses text, so the HTML oracle could not see it; the story screenshot could. Now `set:text={value ?? ''}` on a self-closed tag — `set:text`, not `set:html`, because a textarea's content is never markup. |
| Menu (and SideNavigation through it) | `organisms-navigation-side-navigation--side-navigation` | Drupal menu data uses `below: false` for a leaf, not an absent or empty array. `(item.below ?? []).some(...)` threw on it; `childrenOf()` now coerces. |
<!-- prettier-ignore-end -->

### Visual parity

`npm run test:story-parity:visual` builds the site with `PUBLIC_STORY_PARITY=1`
(which emits the `/story-parity/**` route), then screenshots each page's
`#story-root` and compares it with the upstream Storybook screenshot using
`pixelmatch` at a 0.1% tolerance.

To make the two screenshots comparable, our page reproduces Storybook's own
preview shell — `sb-show-main` plus `sb-main-<layout>`, the rules copied
verbatim into `tests/story-parity/fixtures/shell/storybook-shell.css`, and the
story background colour from `.storybook/preview.js` when the story sets one.
Storybook's shell decides the box `#storybook-root` gets (centred and
shrink-to-fit for `layout: 'centered'`, 1rem body padding for `'padded'`, full
width for `'fullscreen'`), so without it every story would differ.

The capture side is normalised in the same spirit as the HTML oracle: the
`.story-docs` block that `decoratorDocs` injects for `parameters.storyDocs`
stories (alert, skip-link) is removed, and the
`.story-container > .story-container__content` wrapper that
`video-player.stories.js:158` adds as a story-level decorator is unwrapped.
Both are story furniture, absent from `undecoratedStoryFn`'s output, and
`skip-link` renders nothing with a box at all, so both sides record a 1x1
transparent PNG for it.

<!-- prettier-ignore-start -->
| Fix | Story | Gap |
| --- | --- | --- |
| `src/styles/global.scss` | every text-bearing story | CivicTheme's `_variables.base.scss` emits the Lexend and Public Sans `@import url(...)` rules from the MIDDLE of the stylesheet. A CSS `@import` is only valid before every other statement, so the bundler dropped them and the built site fell back to the generic `sans-serif` — while upstream Storybook loaded both fonts. Every text-bearing story rendered several pixels narrower than its fixture. The two `@import`s are now re-stated at the top of `global.scss`, ahead of the CivicTheme import. This is a real SITE fix, not a test shim: without it the template never loads its own typeface. |
<!-- prettier-ignore-end -->

### Whitespace is asymmetrically checked

`normaliseHtml` trims only the LEADING and TRAILING whitespace of each
element's children. A missing whitespace text node BETWEEN two middle siblings
therefore fails the comparison (most of the fixes above are exactly that), but
a missing one at an element's first or last edge does not. The visual suite
does not cover it either, since a single collapsed space rarely moves a pixel.
When adding a separator, note that Prettier rewrites
`<Fragment>…{' '}</Fragment>` into a literal trailing space that the Astro
compiler then drops — use a separate `{condition && ' '}` expression instead
(as `Layout` and `GroupFilter` do), and re-run the suite after `npm run check`.

### Accepted differences

Every entry in `tests/story-parity/accepted-differences.json` cites one of
these rows. Each also carries a `mask`, so the test asserts the difference is
EXACTLY the one described and nothing more (except the `04-templates` rows,
where no component exists to render).

<!-- prettier-ignore-start -->
| Row | Detail |
| --- | --- |
| GroupFilter random group id | `group-filter.twig:69` builds the collapsible group name from `group_id\|default(random(1000, 9999))`. With no `group_id` arg the id is random per render, so a captured fixture and a fresh render can never agree on it. Masked as `ct-group-filter--group-\d+`. |
| ServiceCard stale `summary` story arg | `service-card.stories.js` passes `summary`, which `service-card.twig` neither documents nor reads. Twig ignores an unknown arg; Astro spreads an undeclared prop onto the root element (shared-reference R2), so it prints as an attribute. |
| `04-templates/page` is out of scope | Shared-reference R1 maps only `00-base`, `01-atoms`, `02-molecules` and `03-organisms` to Astro folders. `page.twig` is a Drupal page template; its Astro equivalent is the site's own layout. |
<!-- prettier-ignore-end -->

One earlier "accepted difference" turned out not to be one: the Tabs stories'
`data-tabs-tab` was missing from the captured HTML because
`tabs.stories.js:61` passes `attributes: new DrupalAttribute([...])` and the
capture serialised that `Map` subclass to `{}`. The capture script now unwraps
`Map` args before writing them (see `fixtures/SOURCE.md`), and both Tabs
stories match exactly.

Two existing rows are also cited: **Icon** (`assets_dir` has no Astro prop —
symbols resolve through the generated `icons.ts` map, so the story's
`assets_dir` arg falls into the rest spread) and **(convention)**
(upstream renders with `autoescape = false`, so the `iframe` story's
pre-encoded `&amp;` in `url` passes through Twig verbatim while Astro
correctly escapes it).

## Final parity count

All 77 components ported (00-base: 7, 01-atoms: 22, 02-molecules: 30, 03-organisms: 18). Full parity suite: `npx vitest run tests/parity` — **697 tests passing** across 78 test files (parity `.toBe` snapshot comparisons, including `Slide` via the harness's `meta.dir`, plus direct vitest assertions for upstream-unexercised branches, harness self-tests, and Task 14b/15/14c's slot-vs-string-prop parity tests across every Slot-documented component).

## Accessibility (Task 18)

`tests/e2e/a11y.spec.ts` runs `@axe-core/playwright` (`wcag2a`/`wcag2aa`)
over the 10 reference pages, all 18 `/components/<family>` demo pages,
`/accessibility`, `/events`, `/news`, `/publications` (list + one detail
page each), `/search` and `/404.html` — 37 pages, `npm run test:e2e`. Exit
criterion: zero serious/critical violations. All 37 pages pass.

Moderate/minor violations are collected across every page and written to
`test-results/a11y-moderate-minor.json` (a `testInfo().attach`, plus a
direct file write) by the suite's last test, which asserts the total count
equals `DOCUMENTED_MODERATE_MINOR_COUNT` in the spec — **currently 0**,
matching the empty table below. That assertion, not this paragraph, is the
source of truth: if a moderate/minor violation is ever introduced, the test
fails and this file must gain a row (and the constant must be bumped to
match).

<!-- prettier-ignore-start -->
| Page | Rule id | Impact | Nodes | Note |
| --- | --- | --- | --- | --- |
| _(none — 0 moderate/minor violations across all 37 pages, enforced by `tests/e2e/a11y.spec.ts`)_ |  |  |  |  |
<!-- prettier-ignore-end -->

### Fixed (template-level)

<!-- prettier-ignore-start -->
| Where | Fix | Axe rule(s) |
| --- | --- | --- |
| `src/components/ComponentDemoBlock.astro` | The `<pre class="ct-code">` code-sample block has `overflow-x: auto` with no keyboard-reachable content when it scrolls. Added `tabindex="0"`. | `scrollable-region-focusable` |
| `src/lib/component-demos.ts` (`syncEmbeddedTheme`) | Upstream's `TableOfContentsAutomatic--dark` story hardcodes its `content` blob's wrapper as `class="ct-basic-content ct-theme-light"` even on a `theme: dark` story (upstream's own fixture mistake) — light-theme text colour on our dark stage. Loader-level fix: swap the embedded `ct-theme-*` token to match the story's own `theme`. | `color-contrast` |
| `src/pages/components/alerts.astro` (`withSocialLinksAltFix`) | Upstream's `SocialLinks` story has one item with a raw `icon_html: '<img … src="./demo/images/demo1.jpg"/>'` and no `title` — no `alt`, no other accessible name. Derives a label from the item's URL and injects it as the image's `alt`. | `image-alt` |
| `src/pages/components/base.astro` | Upstream's `Atoms/Iframe` story passes no `title`/`attributes` at all (`iframe.twig` has no title concept of its own). Demo-only fallback `title={fixture.name}`. | `frame-title` |
| `src/pages/components/base.astro` | The Video player demo embeds a real YouTube iframe; axe walks into that cross-origin frame and reports YouTube's own internal player markup, which this project neither renders nor controls. Excluded from the scan (`.ct-video-player__wrapper iframe`), not "fixed". | `aria-allowed-attr`, `aria-prohibited-attr`, `button-name` (all inside the third-party frame) |
| `src/pages/components/forms.astro` | The `Select` atom never renders its own label (that's the `Field`/`Label` molecule's job upstream); the standalone atom demo has no such wrapper. Demo-only `aria-label={fixture.name}`. | `select-name` |
| `src/pages/components/forms.astro` | Upstream's `Webform` story's `referenced_webform` arg is a bare text placeholder ("Webform title") standing in for what would, on a real site, be full themed webform HTML. Wrapped in `ct-basic-content ct-theme-<theme>` (CivicTheme's own themed-content wrapper) for the demo. | `color-contrast` |
| `src/lib/component-demos.ts` (`stripDisabledLinkHref`) + `src/pages/components/navigation.astro` (`withRealisticPaginationHrefs`) | `pagination.twig`/`Pagination.astro` print `items.previous/next.href` unconditionally (`is_disabled` is a separate computation) — upstream's own `Pagination`/`List` story fixtures supply a `previous.href` even on page 1, where Prev is disabled. Found by `tests/e2e/a11y.spec.ts`'s disabled-link guard (a genuinely defective disabled link — still a working `<a href>` — cannot hide behind the `.ct-link--disabled` WCAG 1.4.3 exemption below). Fixed at the loader level for the pre-rendered `List.pagination` HTML blob, and at the `navigation.astro` call site (mirroring `Pagination.astro`'s own disabled computation) for the live-rendered `Pagination` demo. | (custom guard, not an axe rule) |
<!-- prettier-ignore-end -->

### Recorded as upstream-inherited (not fixed in the component; excluded from the scan)

Per the markup-fidelity rule, these are reproduced faithfully from vendored
CivicTheme twig/JS and are NOT fixed inside `src/civictheme/components/**`.
`tests/e2e/a11y.spec.ts`'s `EXCLUDE_ALWAYS` documents each with the same
rationale inline.

<!-- prettier-ignore-start -->
| Component (vendored) | Excluded selector | Axe rule(s) | Cause |
| --- | --- | --- | --- |
| `Link` (via `.ct-link--disabled`) | `.ct-link--disabled` | `color-contrast` | `Link.astro:68` (`isDisabled && 'ct-link--disabled'`) — ANY `Link` rendered with `isDisabled`, not just `Pagination`'s prev/next; Pagination is simply the only current call site that passes `isDisabled`. WCAG 1.4.3 explicitly exempts "inactive user interface component" text from the contrast-minimum requirement; axe cannot itself apply that exemption to a CSS-classed (not `disabled`-attribute) inactive link. Guarded, not just excluded: `tests/e2e/a11y.spec.ts` asserts every `.ct-link--disabled` element on every scanned page has no `href` and is not keyboard-focusable, so a genuinely-defective disabled link (still clickable/tabbable) fails the run instead of hiding behind the exemption. |
| `Popover` / `popover.twig` | `.ct-popover__link` | `aria-allowed-attr` | The trigger renders as a plain `<a>` with no `href` (no implicit interactive role); `collapsible.js` sets `aria-expanded` on it regardless. Upstream's own `popover.twig` never sets `role="button"`. |
| `Tabs` / `tabs.twig` | `.ct-tabs__links` | `aria-required-parent`, `aria-allowed-attr` | `tabs.twig` never puts `role="tablist"` on the links `<ul>`. The panels-generated branch's `role="tab"` links then fail `aria-required-parent`; the molecule's own story (`molecules-tabs--tabs`, explicit `links`, no `role`) instead fails `aria-allowed-attr` when `tabs.js` sets `aria-selected`. Same root cause, so no independently-correct fix at either end. |
| `Tooltip` / `tooltip.twig` | `.ct-tooltip__close-button` | `button-name` | The close button is a bare icon-only `@atoms/button` include with no `title`/label param — no prop path (component or demo) can give it an accessible name. |
| `Tabs` / `tabs.twig` | `.ct-tabs__panels__panel` | `color-contrast` | Narrowed from the whole `.ct-tabs__panels` container to the exact node axe reports (`#panel-1-Tabs-dark`, class `ct-tabs__panels__panel`) — the parent has no content of its own besides these panel `<div>`s. `tabs.twig` prints `panel.content` directly with no theme-scoped text colour (unlike `.ct-basic-content`'s `ct-content-theme($theme)`). The molecule's own upstream fixture (`Panel content`, `theme: dark`) reproduces this, so it isn't this project's page authoring at fault. Revisit if `panels[].content` is ever fed page-authored (not story-fixture) HTML — at that point a themed wrapper (same technique as the `Webform` fix above) becomes the right call-site fix instead of an exclusion. |
<!-- prettier-ignore-end -->

### e2e summary

`npm run test:e2e` — 42/42 passing: 4 `behaviours.spec.ts` + 38 `a11y.spec.ts`
(37 per-page serious/critical + disabled-link-guard tests, all clean, plus 1
moderate/minor-artefact test).
