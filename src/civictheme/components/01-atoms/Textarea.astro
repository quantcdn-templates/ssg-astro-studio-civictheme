---
/**
 * CivicTheme Textarea component.
 *
 * Props:
 * - theme: Theme variation (light or dark).
 * - name: DOM name (required).
 * - id: DOM ID attribute.
 * - value: The textarea value.
 * - placeholder: Placeholder text.
 * - rows: Display rows count.
 * - isInvalid: Whether the textarea is invalid.
 * - isDisabled: Whether the textarea is disabled.
 * - isRequired: Whether the textarea is required.
 * - class: Additional CSS classes.
 * - ...rest: Additional HTML attributes, spread onto the root <textarea>.
 */
interface Props {
  theme?: 'light' | 'dark';
  name?: string;
  id?: string;
  value?: string;
  placeholder?: string;
  rows?: number;
  isInvalid?: boolean;
  isDisabled?: boolean;
  isRequired?: boolean;
  class?: string;
  [key: string]: unknown;
}

const {
  theme = 'light',
  name,
  id,
  value,
  placeholder,
  rows,
  isInvalid,
  isDisabled,
  isRequired,
  class: className = '',
  ...rest
} = Astro.props;

const themeClass = `ct-theme-${theme}`;
const classes = ['ct-textarea', themeClass, isInvalid && 'ct-textarea--is-invalid', className]
  .filter(Boolean)
  .join(' ');
---

{
  name && (
    <textarea
      class={classes}
      name={name}
      rows={rows || undefined}
      id={id || undefined}
      disabled={isDisabled ? 'disabled' : undefined}
      required={isRequired ? 'required' : undefined}
      aria-invalid={isInvalid ? 'true' : undefined}
      placeholder={placeholder || undefined}
      {...rest}
      set:text={value ?? ''}
    />
  )
}
